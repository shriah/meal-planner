---
phase: 21-wire-planboard-curry-override-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/plan/PlanBoard.tsx
  - src/components/plan/PlanBoard.test.tsx
  - .planning/phases/21-wire-planboard-curry-override-flow/21-VALIDATION.md
autonomous: true
requirements:
  - CURRY-05
  - CURRY-08
must_haves:
  truths:
    - "Opening the curry picker from the real weekly plan board provides the selected slot's base-category context."
    - "The board-triggered curry picker continues to represent incompatible curries as explicit override choices rather than normal compatible matches."
    - "Regression coverage fails if the board-to-picker `currentBaseCategoryId` handoff breaks again."
  artifacts:
    - path: "src/components/plan/PlanBoard.tsx"
      provides: "Real board-to-picker handoff of slot base-category context for curry overrides"
    - path: "src/components/plan/PlanBoard.test.tsx"
      provides: "Regression coverage for the board seam that was missed by the v1.3 audit"
    - path: ".planning/phases/21-wire-planboard-curry-override-flow/21-VALIDATION.md"
      provides: "Nyquist validation contract mapping CURRY-05 and CURRY-08 to the corrected board seam plus supporting picker/store proofs"
  key_links:
    - from: "src/components/plan/PlanBoard.tsx"
      to: "src/components/plan/MealPickerSheet.tsx"
      via: "currentBaseCategoryId prop derived from the selected slot's base component"
      pattern: "currentBaseCategoryId"
    - from: "src/components/plan/PlanBoard.test.tsx"
      to: "src/components/plan/PlanBoard.tsx"
      via: "board-triggered curry picker regression assertion"
      pattern: "componentType: 'curry'"
    - from: ".planning/phases/21-wire-planboard-curry-override-flow/21-VALIDATION.md"
      to: "CURRY-05|CURRY-08"
      via: "requirement-to-command traceability"
      pattern: "CURRY-05|CURRY-08"
---

<objective>
Close the v1.3 audit gap in the real PlanBoard entrypoint so explicit curry overrides receive the base-category context the picker already knows how to use.

Purpose: Finish CURRY-05 and CURRY-08 by wiring the missed board-to-picker seam instead of reopening broader compatibility design work.
Output: A narrow `PlanBoard` fix, a regression test that fails on the broken handoff, and a Phase 21 validation contract tied to the board seam plus existing picker/store proofs.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/19-explicit-override-paths/19-01-SUMMARY.md
@.planning/phases/20-compatibility-regression-coverage/20-02-SUMMARY.md
@src/components/plan/PlanBoard.tsx
@src/components/plan/PlanBoard.test.tsx
@src/components/plan/MealPickerSheet.tsx
@src/components/plan/MealPickerSheet.test.tsx
@src/stores/plan-store.ts
@src/stores/plan-store.test.ts

<interfaces>
From src/components/plan/PlanBoard.tsx:
```typescript
const [pickerState, setPickerState] = useState<{
  day: DayOfWeek
  slot: MealSlot
  componentType: 'base' | 'curry' | 'subzi' | 'extras'
} | null>(null)
```

From src/components/plan/MealPickerSheet.tsx:
```typescript
interface MealPickerSheetProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  day: DayOfWeek
  slot: MealSlot
  componentType: 'base' | 'curry' | 'subzi' | 'extras'
  currentBaseCategoryId?: number
}
```

From src/stores/plan-store.ts:
```typescript
swapComponent: (
  day: DayOfWeek,
  slot: MealSlot,
  componentType: 'base' | 'curry' | 'subzi' | 'extras',
  componentId: number,
) => void

regenerate: () => Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the PlanBoard curry picker through the selected slot's base-category context</name>
  <files>src/components/plan/PlanBoard.tsx, src/components/plan/PlanBoard.test.tsx</files>
  <behavior>
    - Test 1: opening the curry picker from a populated board slot forwards that slot's `base_category_id` into `MealPickerSheet`.
    - Test 2: extras continue to receive the same slot-derived `base_category_id`.
    - Test 3: base and subzi pickers stay unchanged and do not receive a forced `currentBaseCategoryId`.
  </behavior>
  <action>Update `PlanBoard` so the selected slot's base component is resolved once and its `base_category_id` is forwarded to the picker for the curry flow as well as extras. Replace the current regression that expects `undefined` for non-extras pickers with assertions that specifically lock the correct curry handoff while preserving existing base/subzi behavior. Keep this fix inside the existing board seam only: do not add new picker props, override metadata, store changes, or any Phase 18/20 cleanup.</action>
  <verify>
    <automated>npx vitest run src/components/plan/PlanBoard.test.tsx</automated>
  </verify>
  <done>The real PlanBoard entrypoint passes slot base context into curry selection, the extras path still works, and the board test suite goes red if that handoff regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-create the Phase 21 validation contract around the closed board seam</name>
  <files>.planning/phases/21-wire-planboard-curry-override-flow/21-VALIDATION.md</files>
  <action>Create a pending Nyquist validation file for Phase 21 that maps `CURRY-05` and `CURRY-08` to the corrected board seam plus the existing `MealPickerSheet` and `plan-store` regression suite. Keep the command set tight: one focused board command, one supporting picker/store command proving explicit override grouping and persistence still hold after the handoff, and the normal phase gate. Do not pull in Phase 18 tech-debt cleanup or malformed Phase 20 metadata cleanup.</action>
  <verify>
    <automated>rg -n "status: pending|CURRY-05|CURRY-08|PlanBoard.test.tsx|MealPickerSheet.test.tsx|plan-store.test.ts|npm test" .planning/phases/21-wire-planboard-curry-override-flow/21-VALIDATION.md</automated>
  </verify>
  <done>Phase 21 has an executable validation artifact that traces the audit gap to the exact board regression and the supporting picker/store proofs without widening scope.</done>
</task>

</tasks>

<verification>
Focused board and validation commands pass:
- `npx vitest run src/components/plan/PlanBoard.test.tsx`
- `rg -n "status: pending|CURRY-05|CURRY-08|PlanBoard.test.tsx|MealPickerSheet.test.tsx|plan-store.test.ts|npm test" .planning/phases/21-wire-planboard-curry-override-flow/21-VALIDATION.md`
</verification>

<success_criteria>
The v1.3 audit gap is closed: the real board entrypoint now gives the curry picker the slot base context it needs, manual override semantics still rely on the existing picker/store contract, and regression coverage explicitly breaks if that handoff is lost again.
</success_criteria>

<output>
After completion, create `.planning/phases/21-wire-planboard-curry-override-flow/21-01-SUMMARY.md`
</output>
