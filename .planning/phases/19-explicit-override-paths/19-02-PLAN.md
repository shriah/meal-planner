---
phase: 19-explicit-override-paths
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/generator.ts
  - src/services/generator.test.ts
  - .planning/phases/19-explicit-override-paths/19-VALIDATION.md
autonomous: true
requirements:
  - CURRY-06
must_haves:
  truths:
    - "A slot-scoped explicit rule can intentionally force an incompatible curry/base pairing."
    - "Tag-based explicit curry rules still choose a compatible match first and only cross compatibility when no compatible match satisfies the rule."
    - "Outside manual/locked selections and the explicit `require_one` override path, incompatible curry/base pairings remain blocked by default."
  artifacts:
    - path: "src/services/generator.ts"
      provides: "Scoped explicit override handling on the existing require_one path"
    - path: "src/services/generator.test.ts"
      provides: "Generator regressions for specific-component and tag-based override precedence"
    - path: ".planning/phases/19-explicit-override-paths/19-VALIDATION.md"
      provides: "Phase 19 validation contract and focused command map"
  key_links:
    - from: "src/services/generator.ts"
      to: "src/types/plan.ts"
      via: "CompiledRule target/effect handling for require_one"
      pattern: "kind: 'require_one'"
    - from: "src/services/generator.ts"
      to: "src/services/generator.test.ts"
      via: "Focused explicit-override regressions"
      pattern: "require_one"
---

<objective>
Extend the existing `require_one` rule path so it can act as a narrow explicit compatibility override without relaxing the Phase 18 default contract.

Purpose: Deliver CURRY-06 with the existing rule vocabulary and preserve the milestone promise that compatibility stays strict unless the user explicitly asks for an exception.
Output: Generator-side scoped override behavior, targeted rule regressions, and a Phase 19 validation contract.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-explicit-override-paths/19-CONTEXT.md
@.planning/phases/19-explicit-override-paths/19-RESEARCH.md
@.planning/phases/18-generator-compatibility-contract/18-01-SUMMARY.md
@src/services/generator.ts
@src/services/rule-compiler.ts
@src/types/plan.ts

<interfaces>
From src/types/plan.ts:
```typescript
export type Target =
  | { mode: 'component_type'; component_type: 'base' | 'curry' | 'subzi' }
  | { mode: 'tag'; filter: TagFilter }
  | { mode: 'component'; component_id: number }
  | { mode: 'base_category'; category_id: number }

export type RequireOneEffect = Extract<AnyEffect, { kind: 'require_one' }>
```

From src/services/rule-compiler.ts:
```typescript
export function compileRule(state: RuleFormState): CompiledRule
export function decompileRule(
  compiled: CompiledRule,
  name: string,
  context?: DecompileCategoryContext,
): RuleFormState
```

From src/services/generator.ts:
```typescript
function applyRequireOne(
  currentPick: ComponentRecord | undefined,
  rules: ValidatedRule[],
  pool: ComponentRecord[],
  day: DayOfWeek,
  slot: MealSlot,
  warnings: Warning[],
  fullLibrary?: ComponentRecord[],
): ComponentRecord | undefined
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrowly extend require_one to support explicit incompatible curry overrides</name>
  <files>src/services/generator.ts, src/services/generator.test.ts</files>
  <behavior>
    - Test 1: a specific-component `require_one` targeting an incompatible curry can force that exact curry for the scoped slot.
    - Test 2: a tag-based curry `require_one` stays within compatible matches when one exists, and only falls back to incompatible matches when compatibility would otherwise make the rule fail.
    - Test 3: a broad `component_type: 'curry'` `require_one` remains compatibility-scoped and does not become a generic override loophole.
  </behavior>
  <action>Refactor the curry `require_one` path in `generator.ts` per D-07, D-08, D-09, D-13, and D-14. Keep Phase 18's compatibility narrowing as the default automatic path. Permit explicit compatibility bypass only for intentionally scoped `require_one` rules: exact component targets may resolve from the full curry library, and tag targets may search compatible curries first before an explicit incompatible fallback. Do not add a new rule effect, target mode, boolean override flag, or UI control. Keep manual/locked selections higher precedence than rule overrides per D-12.</action>
  <verify>
    <automated>npx vitest run src/services/generator.test.ts</automated>
  </verify>
  <done>Specific-component and scoped tag `require_one` rules can intentionally create incompatible curry/base pairings, while broad or default generator flows still reject incompatible curries.</done>
</task>

<task type="auto">
  <name>Task 2: Draft the Phase 19 validation contract around explicit override behavior</name>
  <files>.planning/phases/19-explicit-override-paths/19-VALIDATION.md</files>
  <action>Create `19-VALIDATION.md` as the phase command map after the code and tests are in place. Map CURRY-05 and CURRY-06 to focused picker, store, and generator commands, and make the expected evidence explicit: grouped picker behavior, manual/locked incompatible persistence, scoped `require_one` override behavior, and no default compatibility regression. Do not mark the file approved until the listed commands have actually been rerun during execution.</action>
  <verify>
    <automated>test -f .planning/phases/19-explicit-override-paths/19-VALIDATION.md</automated>
  </verify>
  <done>The phase has a concrete validation artifact with requirement-to-command traceability and explicit rerun expectations for the override paths.</done>
</task>

</tasks>

<verification>
Focused runtime and phase checks pass:
- `npx vitest run src/services/generator.test.ts`
- `npm test`
</verification>

<success_criteria>
CURRY-06 is satisfied: explicit scoped `require_one` rules can override curry compatibility when intended, tag rules remain compatibility-first, and the Phase 18 compatible-by-default contract still holds everywhere else.
</success_criteria>

<output>
After completion, create `.planning/phases/19-explicit-override-paths/19-02-SUMMARY.md`
</output>
