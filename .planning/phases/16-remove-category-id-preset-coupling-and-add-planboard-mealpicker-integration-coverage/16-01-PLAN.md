---
phase: 16-remove-category-id-preset-coupling-and-add-planboard-mealpicker-integration-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/rules/form-state.ts
  - src/components/rules/RuleForm.tsx
  - src/components/rules/form-state.test.ts
  - src/components/plan/PlanBoard.tsx
  - src/components/plan/PlanBoard.test.tsx
autonomous: true
gap_closure: true
requirements: [audit-tech-debt-phase-14-category-coupling-and-picker-handoff]
must_haves:
  truths:
    - "Loading the `rice-lunch-dinner` preset no longer depends on the rice base category having numeric ID `1`."
    - "Preset-backed base-category rules resolve against the built-in category identity, not seed insertion order."
    - "Opening the extras picker from `PlanBoard` passes the selected slot's base category ID into `MealPickerSheet`."
    - "Automated coverage directly exercises the `PlanBoard -> MealPickerSheet` extras handoff instead of relying only on picker-level inference."
  artifacts:
    - path: "src/components/rules/form-state.ts"
      provides: "Preset definition/resolution that uses stable built-in category identity instead of hard-coded category IDs"
    - path: "src/components/rules/RuleForm.tsx"
      provides: "Preset loading flow that hydrates category-targeted presets from live category rows before dispatch"
    - path: "src/components/plan/PlanBoard.test.tsx"
      provides: "Direct regression coverage for the `currentBaseCategoryId` prop passed from `PlanBoard` to `MealPickerSheet`"
  key_links:
    - from: "src/components/rules/RuleForm.tsx"
      to: "src/components/rules/form-state.ts"
      via: "Preset lookup/resolution before LOAD_PRESET dispatch"
      pattern: "LOAD_PRESET|preset"
    - from: "src/components/plan/PlanBoard.tsx"
      to: "src/components/plan/MealPickerSheet.tsx"
      via: "`currentBaseCategoryId` prop on extras picker open"
      pattern: "currentBaseCategoryId"
    - from: "src/components/plan/PlanBoard.test.tsx"
      to: "src/components/plan/PlanBoard.tsx"
      via: "Rendered interaction plus mocked picker prop assertions"
      pattern: "currentBaseCategoryId|MealPickerSheet"
---

<objective>
Close the two remaining v1.2 Phase 14 audit debt items without changing product scope: remove the hidden category-ID preset coupling from the rules form and add direct automated coverage for the `PlanBoard -> MealPickerSheet` extras-category handoff.

Purpose: The milestone audit found a brittle implementation assumption and an indirect integration test gap. Phase 16 should eliminate the hard-coded preset dependency on seed ordering and prove the plan-board picker wiring with dedicated automated coverage.
Output: One focused code change set that resolves preset category IDs from stable built-in category identity and adds a direct regression test for `PlanBoard -> MealPickerSheet`.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VERIFICATION.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-03-SUMMARY.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-04-SUMMARY.md
@src/types/category.ts
@src/db/seed-data.ts
@src/components/rules/form-state.ts
@src/components/rules/form-state.test.ts
@src/components/rules/RuleForm.tsx
@src/components/plan/PlanBoard.tsx
@src/components/plan/PlanBoard.test.tsx
@src/components/plan/MealPickerSheet.tsx
@src/components/plan/MealPickerSheet.test.tsx

<interfaces>
From `src/components/rules/form-state.ts`:
```typescript
export const EXAMPLE_PRESETS: Record<string, RuleFormState> = {
  'rice-lunch-dinner': {
    name: 'Rice: lunch and dinner only',
    target: { mode: 'base_category', base_category_id: 1 },
    allowed_slots: ['lunch', 'dinner'],
  },
};
```

From `src/components/rules/RuleForm.tsx`:
```typescript
useEffect(() => {
  if (presetLoadedRef.current) return;
  const preset = searchParams.get('preset');
  if (preset && EXAMPLE_PRESETS[preset]) {
    dispatch({ type: 'LOAD_PRESET', state: EXAMPLE_PRESETS[preset] });
    presetLoadedRef.current = true;
  }
}, [searchParams]);
```

From `src/components/plan/MealPickerSheet.tsx`:
```typescript
interface MealPickerSheetProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  day: DayOfWeek
  slot: MealSlot
  componentType: 'base' | 'curry' | 'subzi' | 'extras'
  currentBaseCategoryId?: number
}
```

From `src/components/plan/PlanBoard.tsx`:
```typescript
currentBaseCategoryId={
  pickerState.componentType === 'extras'
    ? (() => {
        const ps = plan?.slots.find(s => s.day === pickerState.day && s.meal_slot === pickerState.slot)
        const baseComp = ps ? componentsMap.get(ps.base_id) : undefined
        return baseComp?.base_category_id ?? undefined
      })()
    : undefined
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hard-coded preset category IDs with stable preset resolution</name>
  <files>src/components/rules/form-state.ts, src/components/rules/RuleForm.tsx, src/components/rules/form-state.test.ts</files>
  <behavior>
    - Test 1: The `rice-lunch-dinner` preset resolves to the live built-in `rice-based` category ID even when that category's ID is not `1`.
    - Test 2: Preset loading does not dispatch a wrong base-category target before category resolution is available.
  </behavior>
  <action>
    Remove the hidden numeric-ID assumption called out in the audit by changing the preset flow to resolve category-targeted presets from stable built-in category identity, per the Phase 14 category model.

    Implement this as a tightly scoped preset-resolution change:
    - keep preset definitions declarative in `src/components/rules/form-state.ts`, but stop hard-coding `base_category_id: 1`
    - store the built-in category identity in a stable way derived from `src/types/category.ts` / seeded built-in names rather than seed insertion order
    - export a pure resolver/helper from `form-state.ts` that turns a preset slug plus live base-category rows into a concrete `RuleFormState`
    - update `src/components/rules/RuleForm.tsx` to use that resolver before dispatching `LOAD_PRESET`, waiting for the necessary category row instead of dispatching an incorrect numeric ID
    - keep the fix in the rules form/preset boundary; do not broaden into category-service or seed refactors unless a tiny shared helper is strictly required
    - do not reintroduce direct Dexie access inside `form-state.ts`; preserve the existing pure-module boundary

    Extend `src/components/rules/form-state.test.ts` so the regression proves preset resolution works with non-`1` category IDs and explicitly guards against the old coupling.
  </action>
  <verify>
    <automated>npx vitest run src/components/rules/form-state.test.ts</automated>
  </verify>
  <done>The rules preset path no longer assumes built-in category order, and the `rice-lunch-dinner` preset resolves by category identity instead of a raw numeric ID.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add direct PlanBoard-to-MealPicker extras handoff coverage</name>
  <files>src/components/plan/PlanBoard.tsx, src/components/plan/PlanBoard.test.tsx</files>
  <behavior>
    - Test 1: Opening the extras picker for a populated slot passes that slot's base component `base_category_id` into `MealPickerSheet`.
    - Test 2: Opening a non-extras picker keeps `currentBaseCategoryId` undefined.
  </behavior>
  <action>
    Add the missing direct automated integration coverage for the wiring already implemented in Phase 14, without widening into unrelated PlanBoard refactors.

    Prefer extending `src/components/plan/PlanBoard.test.tsx` so it verifies the actual parent-to-child handoff:
    - render `PlanBoard` with a real plan slot and a `componentsMap` scenario where the selected base component has a known `base_category_id`
    - mock or spy on `MealPickerSheet` so the test can assert the props PlanBoard passes when the user opens a picker from a slot
    - exercise the extras path specifically and assert `currentBaseCategoryId` matches the base component's category ID
    - also assert a non-extras picker path leaves `currentBaseCategoryId` undefined so the prop stays extras-only
    - keep the existing picker-level query-path coverage in `MealPickerSheet.test.tsx` as complementary evidence; do not replace it with another indirect assertion

    Only touch `src/components/plan/PlanBoard.tsx` if the test needs a very small extraction to make the prop computation inspectable. Do not redesign picker state, store wiring, or component structure.
  </action>
  <verify>
    <automated>npx vitest run src/components/plan/PlanBoard.test.tsx src/components/plan/MealPickerSheet.test.tsx</automated>
  </verify>
  <done>There is a dedicated automated regression test proving `PlanBoard` passes the current slot's base category ID into `MealPickerSheet` for extras and only for extras.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/components/rules/form-state.test.ts` and `npx vitest run src/components/plan/PlanBoard.test.tsx src/components/plan/MealPickerSheet.test.tsx`. Confirm the focused suite captures both audit debts: preset resolution no longer relies on category ID `1`, and `PlanBoard` directly proves the `currentBaseCategoryId` handoff into `MealPickerSheet`.
</verification>

<success_criteria>
The v1.2 milestone audit has no remaining open Phase 14 debt: rule presets are decoupled from built-in category insertion order, and the `PlanBoard -> MealPickerSheet` category handoff is covered by a dedicated passing automated test rather than inference alone.
</success_criteria>

<output>
After completion, create `.planning/phases/16-remove-category-id-preset-coupling-and-add-planboard-mealpicker-integration-coverage/16-01-SUMMARY.md`
</output>
