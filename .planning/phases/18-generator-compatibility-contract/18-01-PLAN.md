---
phase: 18-generator-compatibility-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/generator.ts
  - src/services/generator.test.ts
  - src/components/plan/PlanBoard.test.tsx
  - .planning/phases/18-generator-compatibility-contract/18-VALIDATION.md
autonomous: true
requirements: [CURRY-03, CURRY-04]
must_haves:
  truths:
    - "Newly generated meal slots only auto-select curries that are compatible with the base chosen for that slot."
    - "If no compatible curry exists for a slot, the generator leaves `curry_id` unset instead of silently inserting an incompatible curry."
    - "Compatibility-respecting curry omission still appears through the existing warning banner and slot-warning UI without changing manual or locked picker behavior."
  artifacts:
    - path: "src/services/generator.ts"
      provides: "Compatibility-scoped automatic curry selection with skip-and-warn behavior"
    - path: "src/services/generator.test.ts"
      provides: "Regression coverage for compatibility narrowing, zero-compatible curries, skip-and-warn behavior, and require_one scoping"
    - path: "src/components/plan/PlanBoard.test.tsx"
      provides: "UI regression coverage proving existing warning plumbing surfaces omitted-curry warnings"
    - path: ".planning/phases/18-generator-compatibility-contract/18-VALIDATION.md"
      provides: "Pre-created Nyquist validation contract kept aligned with execution results"
  key_links:
    - from: "src/services/generator.ts"
      to: "src/services/generator.test.ts"
      via: "Focused generator regressions for compatibility-scoped auto curry selection"
      pattern: "compatible_base_category_ids|curry_id|require_one|warning"
    - from: "src/services/generator.ts"
      to: "src/types/plan.ts"
      via: "Existing `Warning` result objects carry omitted-curry messages without a new result shape"
      pattern: "Warning|GeneratorResult|message"
    - from: "src/components/plan/PlanBoard.test.tsx"
      to: "src/components/plan/MealCell.tsx"
      via: "Slot warnings flow through the existing PlanBoard -> MealCell warning path"
      pattern: "warnings|Warning|highlighted cell"
---

<objective>
Make curry/base compatibility a hard default generator contract: automatic curry selection must stay inside the chosen base's compatible set, and zero-compatible outcomes must omit curry instead of relaxing back to an incompatible fallback.

Purpose: Phase 17 shipped the persisted compatibility data, but the generator still ignores it. Phase 18 converts that data into default runtime behavior while preserving the explicit override boundary for Phase 19.
Output: Generator enforcement, focused regression tests, and a draft phase validation artifact.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-generator-compatibility-contract/18-CONTEXT.md
@.planning/phases/18-generator-compatibility-contract/18-RESEARCH.md
@.planning/phases/17-curry-compatibility-data/17-03-SUMMARY.md
@.planning/phases/17-curry-compatibility-data/17-VERIFICATION.md
@.planning/milestones/v1.2-phases/13-only-include-extras-when-explicitly-required/13-01-PLAN.md
@src/services/generator.ts
@src/services/generator.test.ts
@src/types/plan.ts
@src/components/plan/PlanBoard.tsx
@src/components/plan/MealCell.tsx
@src/components/plan/PlanBoard.test.tsx
@src/components/plan/MealPickerSheet.test.tsx

<interfaces>
From `src/types/plan.ts`:
```typescript
export interface PlanSlot {
  day: DayOfWeek;
  meal_slot: MealSlot;
  base_id: number;
  curry_id?: number;
  subzi_id?: number;
  extra_ids: number[];
}

export interface Warning {
  slot: { day: DayOfWeek; meal_slot: MealSlot };
  rule_id: number | null;
  message: string;
}

export interface GeneratorResult {
  plan: WeeklyPlan;
  warnings: Warning[];
}
```

From `src/services/generator.ts`:
```typescript
export interface GenerateOptions {
  lockedSlots?: Partial<Record<`${DayOfWeek}-${MealSlot}`, {
    base_id?: number;
    curry_id?: number;
    subzi_id?: number;
    extra_ids?: number[];
  }>>;
}

function applyRequireOne(
  selected: ComponentRecord,
  rules: ValidatedRule[],
  fullLibrary: ComponentRecord[],
  day: DayOfWeek,
  slot: MealSlot,
  warnings: Warning[],
): ComponentRecord { ... }
```

From `src/components/plan/MealCell.tsx`:
```typescript
export function MealCell({ day, slot, planSlot, componentsMap, warnings, onPickerOpen }: MealCellProps) {
  const cellWarnings = warnings.filter(w => w.slot.day === day && w.slot.meal_slot === slot);
  const hasWarning = cellWarnings.length > 0;
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce compatibility-scoped automatic curry selection without any relax-to-incompatible fallback</name>
  <files>src/services/generator.ts, src/services/generator.test.ts</files>
  <behavior>
    - Test 1: per D-04, CURRY-03, and the Phase 17 data contract, an auto-generated curry comes only from curries whose `compatible_base_category_ids` include the selected base category ID.
    - Test 2: per D-01 through D-03 and CURRY-04, when compatibility leaves no eligible curry candidates, the slot keeps its base and `curry_id` stays unset while a warning is recorded.
    - Test 3: per D-10 through D-12, a curry with `compatible_base_category_ids: []` never enters the automatic curry pool.
    - Test 4: per D-05, D-06, and the research pitfall note, `require_one` cannot backdoor an incompatible curry from the full curry library during Phase 18.
    - Test 5: per D-13 through D-15, locked/manual curry paths remain unchanged and are not compatibility-filtered in this phase.
  </behavior>
  <action>
    Update `src/services/generator.ts` so the automatic curry branch narrows candidates using the already-selected base before weighted selection. Implement this as a dedicated hard compatibility constraint per D-04, not by reusing relaxable rule helpers as the compatibility mechanism. The narrowed automatic pool must preserve the Phase 17 distinction between `compatible_base_category_ids: []` and legacy/missing data per D-10 through D-12.

    Keep the locked `curry_id` branch untouched per D-13 through D-15. When no compatible automatic curry exists, do not broaden back to all curries, do not treat zero-compatible as unrestricted, and do not add override semantics reserved for Phase 19. Instead, reuse the existing `Warning` result path per D-07 through D-09, leave `curry_id` unset, and keep the rest of the slot intact.

    Adjust `applyRequireOne()` usage for curries so any require-one override remains inside the compatibility-scoped curry library during this phase. That can be done either by passing a compatibility-scoped curry library or by introducing a small curry-specific wrapper, but the result must stay readable and testable. Extend `src/services/generator.test.ts` first to cover compatibility selection, explicit-empty arrays, no-compatible skip-and-warn behavior, require-one scoping, and unchanged locked/manual behavior.
  </action>
  <verify>
    <automated>npx vitest run src/services/generator.test.ts</automated>
  </verify>
  <done>Automatic generation only produces compatible curries by default, omitted curries leave `curry_id` unset with a slot warning, explicit `[]` curries never auto-generate, and locked/manual behavior remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the existing warning surface and keep the phase validation contract aligned</name>
  <files>src/components/plan/PlanBoard.test.tsx, .planning/phases/18-generator-compatibility-contract/18-VALIDATION.md</files>
  <action>
    Add a focused regression in `src/components/plan/PlanBoard.test.tsx` that seeds store warnings with omitted-curry copy and proves the existing warning UI still responds through the current banner/cell path, per D-07 through D-09. Reuse the existing warning presentation only; do not add a new banner type, stronger styling branch, or picker restriction logic per D-08 and D-14.

    Update `.planning/phases/18-generator-compatibility-contract/18-VALIDATION.md`, which already exists from planning, so its command map, scope notes, and status stay aligned with the shipped execution results. Keep the status pending during execution until focused commands and the full phase gate pass, then promote it during phase verification. The validation contract must continue to state that manual picker and override behavior remain Phase 19 scope, while existing `MealPickerSheet` coverage is rerun only as a regression guard for the untouched path.
  </action>
  <verify>
    <automated>npx vitest run src/components/plan/PlanBoard.test.tsx src/components/plan/MealPickerSheet.test.tsx && rg -n "status: pending|CURRY-03|CURRY-04|npm test" .planning/phases/18-generator-compatibility-contract/18-VALIDATION.md</automated>
  </verify>
  <done>The existing warning UI has a regression proving omitted-curry warnings still surface without new UI types, and the phase has a draft validation artifact with focused commands plus a full-suite gate.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/services/generator.test.ts` after the generator changes, then `npx vitest run src/components/plan/PlanBoard.test.tsx src/components/plan/MealPickerSheet.test.tsx` after the warning regression and validation artifact update. Finish with `npm test` as the phase gate. Phase 18 only passes when auto-generation cannot produce an incompatible curry by default, zero-compatible outcomes omit curry with a warning, and manual/locked override behavior remains untouched for Phase 19.
</verification>

<success_criteria>
CURRY-03 and CURRY-04 are fully covered by a single executable plan: automatic curry selection is compatibility-scoped by the chosen base, no-compatible slots keep `curry_id` unset with existing warning plumbing, explicit zero-compatible curries never auto-generate, and no Phase 19 override behavior is pulled forward.
</success_criteria>

<output>
After completion, create `.planning/phases/18-generator-compatibility-contract/18-01-SUMMARY.md`
</output>
