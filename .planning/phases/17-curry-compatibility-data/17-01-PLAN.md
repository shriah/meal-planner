---
phase: 17-curry-compatibility-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/component.ts
  - src/db/seed-data.ts
  - src/db/seed.test.ts
autonomous: true
requirements: [CURRY-01, CURRY-02]
must_haves:
  truths:
    - "Fresh installs get explicit editable curry compatibility data backed by base-category IDs, not hidden inferred rules."
    - "Seeded/default curries arrive with curated compatibility arrays that later migration and Library UI can reuse consistently."
    - "A zero-compatible curry remains intentionally representable as an explicit empty array instead of being collapsed into missing data."
  artifacts:
    - path: "src/types/component.ts"
      provides: "Curry record shape with category-ID-backed compatibility storage"
    - path: "src/db/seed-data.ts"
      provides: "Curated seeded curry compatibility resolver/materialization path"
    - path: "src/db/seed.test.ts"
      provides: "Seed regression coverage for curated curry compatibility data"
  key_links:
    - from: "src/db/seed-data.ts"
      to: "src/types/component.ts"
      via: "Seed materialization writes `compatible_base_category_ids` on curry records"
      pattern: "compatible_base_category_ids"
    - from: "src/db/seed.test.ts"
      to: "src/db/seed-data.ts"
      via: "Fresh-seed assertions for curated curry/base mappings"
      pattern: "compatible_base_category_ids|curry"
---

<objective>
Define the shared curry compatibility storage contract and curated seed mapping seam that the migration and Library UI will both build on.

Purpose: Phase 17 needs one category-ID-safe curry model, not separate fresh-seed and upgrade-path behaviors. This plan establishes the persisted field, exact seeded mapping rules, and regression coverage for the seeded/default path before downstream migration/UI work begins.
Output: Curry record typing plus seeded curry materialization that carries explicit `compatible_base_category_ids`.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-curry-compatibility-data/17-CONTEXT.md
@.planning/phases/17-curry-compatibility-data/17-RESEARCH.md
@.planning/milestones/v1.2-phases/14-add-option-to-create-more-base-category-and-extra-category/14-CONTEXT.md
@.planning/milestones/v1.2-phases/14-add-option-to-create-more-base-category-and-extra-category/14-04-SUMMARY.md
@src/types/component.ts
@src/types/category.ts
@src/db/seed-data.ts
@src/db/seed.test.ts

<interfaces>
From `src/types/component.ts`:
```typescript
export interface CurryRecord extends BaseComponentFields {
  componentType: 'curry';
  curry_category?: string;
}

export type ComponentRecord = BaseComponentFields & {
  curry_category?: string;
  compatible_base_category_ids?: number[];
};
```

From `src/db/seed-data.ts`:
```typescript
export type SeedCategoryLookup = {
  base: Record<BaseType, number>;
  extra: Record<ExtraCategory, number>;
};

function applySeedCategoryIds(
  component: Omit<ComponentRecord, 'id'>,
  categoryLookup: SeedCategoryLookup,
): Omit<ComponentRecord, 'id'> { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the curry record contract and prove seeded curries carry explicit compatibility arrays</name>
  <files>src/types/component.ts, src/db/seed.test.ts</files>
  <behavior>
    - Test 1: per D-01 and CURRY-02, fresh seed output stores curated `compatible_base_category_ids` on curry records rather than leaving the field undefined.
    - Test 2: per D-04 and D-05, the curry type contract allows an explicit empty array so "compatible with none" stays distinct from legacy missing data.
  </behavior>
  <action>
    Update the shared component typing so curries can persist `compatible_base_category_ids` using the same numeric-ID contract already used for extras, per D-01 and the phase requirement summary. Keep the field optional at the flat `ComponentRecord` layer so the migration can still detect legacy `undefined`, but make the curry-facing contract explicit enough that later UI code can treat `[]` as an intentional value instead of a missing field.

    Extend `src/db/seed.test.ts` first so the seed path must prove seeded curries now carry explicit compatibility data and that the stored shape remains category-ID-based. Do not add any fallback-to-all-bases assertions in this plan; that upgrade-only behavior belongs to the migration plan.
  </action>
  <verify>
    <automated>npx vitest run src/db/seed.test.ts</automated>
  </verify>
  <done>Curry typing and seed tests both reflect an explicit `compatible_base_category_ids` contract, including support for an intentional empty array.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize curated seeded curry compatibility with an exact-match resolver</name>
  <files>src/db/seed-data.ts, src/db/seed.test.ts</files>
  <read_first>
    - src/db/seed-data.ts
    - src/db/seed.test.ts
    - src/types/component.ts
    - src/types/category.ts
    - .planning/phases/17-curry-compatibility-data/17-CONTEXT.md
    - .planning/phases/17-curry-compatibility-data/17-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: per D-01, seeded/default curries receive curated compatible base-category IDs.
    - Test 2: per D-03 and the research pitfall guidance, matching uses only exact seeded identifiers you control; user-renamed or unmatched curries are not narrowed here.
    - Test 3: per D-04, a curated empty array remains persisted as an intentional zero-compatible curry state.
  </behavior>
  <action>
    Implement the seeded curry mapping seam in `src/db/seed-data.ts` so fresh installs and the later Dexie migration can rely on the same curated compatibility knowledge. Keep the seed fixtures human-readable per the Phase 14 pattern, but add an exact-match mapping helper keyed only to seeded/default curry identities you control. Use category IDs resolved through the existing `SeedCategoryLookup`, not labels stored directly on records, and preserve empty arrays when a curated mapping intentionally marks a curry as auto-pick-ineligible per D-04 through D-06.

    Do not add fuzzy name matching, generator enforcement, override semantics, or composition-mode logic. Anything not matched by the curated seeded map stays a migration concern for Plan 17-02, not a fresh-seed heuristic here.
  </action>
  <verify>
    <automated>npx vitest run src/db/seed.test.ts</automated>
  </verify>
  <done>Fresh seed output contains curated curry compatibility arrays backed by base category IDs, with no loose-matching logic and no ambiguity between `undefined` and `[]`.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/db/seed.test.ts`. Confirm seeded curries persist explicit `compatible_base_category_ids`, the values are numeric category IDs, and the contract still allows an intentional empty array without conflating it with legacy missing data.
</verification>

<success_criteria>
Phase 17 has a reusable curry compatibility data contract: seeded/default curries materialize curated base-category IDs, the shared type surface can represent explicit zero-compatible curries, and downstream migration/UI work can consume the same field without redefining semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/17-curry-compatibility-data/17-01-SUMMARY.md`
</output>
