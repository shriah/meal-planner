---
phase: 17-curry-compatibility-data
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/components/library/ComponentForm.tsx
  - src/components/library/ComponentForm.test.tsx
  - src/components/library/ComponentRow.tsx
  - src/components/library/ComponentRow.test.tsx
  - .planning/phases/17-curry-compatibility-data/17-VALIDATION.md
autonomous: true
requirements: [CURRY-01, CURRY-07]
must_haves:
  truths:
    - "Users can create or edit a curry in the Library and choose one or more compatible base categories."
    - "Collapsed curry rows show category-label summaries derived from current base-category records rather than stored labels."
    - "Zero-compatible curries remain editable but are clearly marked as not eligible for auto-selection."
  artifacts:
    - path: "src/components/library/ComponentForm.tsx"
      provides: "Library curry checklist editing and zero-compatible warning state"
    - path: "src/components/library/ComponentRow.tsx"
      provides: "Collapsed curry compatibility summary and warning badge"
    - path: "src/components/library/ComponentRow.test.tsx"
      provides: "Dedicated regression coverage for collapsed curry labels and zero-compatible state"
    - path: ".planning/phases/17-curry-compatibility-data/17-VALIDATION.md"
      provides: "Nyquist validation contract for the finished phase"
  key_links:
    - from: "src/components/library/ComponentForm.tsx"
      to: "src/services/category-db.ts"
      via: "Live base-category checklist rendered from current category rows"
      pattern: "getCategoriesByKind|compatible_base_category_ids"
    - from: "src/components/library/ComponentRow.tsx"
      to: "src/lib/category-labels.ts"
      via: "Collapsed summary labels resolved from current base-category records"
      pattern: "getBaseCategoryLabel|compatible_base_category_ids"
    - from: "src/components/library/ComponentRow.test.tsx"
      to: "src/components/library/ComponentRow.tsx"
      via: "Row-level assertions for summary labels and zero-compatible badge copy"
      pattern: "compatible|warning|badge"
---

<objective>
Expose the new curry compatibility model in the Library using the existing checklist and collapsed-summary patterns, then lock the phase down with explicit validation commands.

Purpose: Phase 17 is only valuable if users can see and edit the stored data. This plan reuses the proven library-native UX from extras, adds the required zero-compatible warning state, and records the phase validation contract around the DB and UI changes.
Output: Curry checklist editing, collapsed-row compatibility summaries, warning badges, and a phase validation artifact.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-curry-compatibility-data/17-CONTEXT.md
@.planning/phases/17-curry-compatibility-data/17-RESEARCH.md
@.planning/phases/17-curry-compatibility-data/17-01-SUMMARY.md
@src/components/library/ComponentForm.tsx
@src/components/library/ComponentForm.test.tsx
@src/components/library/ComponentRow.tsx
@src/lib/category-labels.ts
@src/services/category-db.ts
@src/types/component.ts

<interfaces>
From `src/components/library/ComponentForm.tsx`:
```typescript
interface FormState {
  name: string;
  base_category_id: string;
  extra_category_id: string;
  compatible_base_category_ids: number[];
}
```

From `src/components/library/ComponentRow.tsx`:
```typescript
const compatibleBaseLabels = (component.compatible_base_category_ids ?? []).map((id) =>
  getBaseCategoryLabel(baseCategoriesById, id),
);
```

From `src/lib/category-labels.ts`:
```typescript
export function getBaseCategoryLabel(
  categoriesById: Map<number, CategoryRecord>,
  id: number | null | undefined,
): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add curry compatibility checklist editing and an explicit zero-compatible warning in the Library form</name>
  <files>src/components/library/ComponentForm.tsx, src/components/library/ComponentForm.test.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
    - node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md
    - src/components/library/ComponentForm.tsx
    - src/components/library/ComponentForm.test.tsx
    - src/types/component.ts
    - src/services/category-db.ts
    - .planning/phases/17-curry-compatibility-data/17-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: per D-07 and CURRY-01, curry create/edit reuses the existing compatible-base checklist pattern instead of introducing a new picker flow.
    - Test 2: per D-04 through D-06, saving a curry with no selected base categories persists `[]` and shows clear warning copy that the curry will not be auto-selected.
    - Test 3: per CURRY-07, rename-driven label updates still render through live base-category records rather than stored names.
  </behavior>
  <action>
    Extend `src/components/library/ComponentForm.tsx` so curries participate in the same inline compatible-base checklist pattern currently used for extras, per D-07 through D-09. Reuse the existing `compatible_base_category_ids` form state instead of inventing a new data path, and make the section available when `componentType === 'curry'`. Keep `[]` as a valid saved value per D-04/D-05, and add explicit warning copy in the form whenever a curry has zero selected compatible bases so users understand the curry will not be auto-selected.

    Update `src/components/library/ComponentForm.test.tsx` first so it covers curry add/edit flows, persisted empty arrays, and rename-safe label rendering. Do not add generator enforcement or override messaging in this phase.
  </action>
  <verify>
    <automated>npx vitest run src/components/library/ComponentForm.test.tsx</automated>
  </verify>
  <done>The Library form lets users assign base-category compatibility to curries, preserves intentional zero-compatible states, and surfaces an explicit warning instead of silently treating the state as broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Show collapsed curry compatibility summaries and record the phase validation contract</name>
  <files>src/components/library/ComponentRow.tsx, src/components/library/ComponentRow.test.tsx, .planning/phases/17-curry-compatibility-data/17-VALIDATION.md</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
    - node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md
    - src/components/library/ComponentRow.tsx
    - src/components/library/ComponentForm.test.tsx
    - src/lib/category-labels.ts
    - .planning/milestones/v1.2-phases/14-add-option-to-create-more-base-category-and-extra-category/14-04-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: per D-08 and CURRY-01, collapsed curry rows show the selected compatible base labels using live category-label resolution.
    - Test 2: per D-06 and D-11, a zero-compatible curry shows a clear warning/badge in the collapsed row instead of disappearing or being expanded back to all bases.
    - Test 3: the phase validation artifact lists the real DB/UI commands for CURRY-01, CURRY-02, and CURRY-07 and marks the wave coverage needed for approval.
  </behavior>
  <action>
    Update `src/components/library/ComponentRow.tsx` so curries mirror the existing compatibility-summary pattern used elsewhere in the library, per D-08. Resolve labels through the current category map, not stored names, and add a visible badge/warning state for zero-compatible curries that aligns with the form warning without implying a generator override. Create `src/components/library/ComponentRow.test.tsx` to cover collapsed label rendering, rename-safe updates, and the zero-compatible badge state.

    Then create `.planning/phases/17-curry-compatibility-data/17-VALIDATION.md` with the focused command map for this phase. It should include the real automated commands for seed, migration, service-normalization, and library UI coverage so Phase 17 closes with an explicit Nyquist validation contract rather than relying on ad hoc reruns.
  </action>
  <verify>
    <automated>npx vitest run src/components/library/ComponentForm.test.tsx src/components/library/ComponentRow.test.tsx</automated>
  </verify>
  <done>Collapsed curry rows summarize compatible base labels, zero-compatible curries are visibly warned in the Library, and the phase has a committed validation artifact naming the exact approval commands.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/components/library/ComponentForm.test.tsx src/components/library/ComponentRow.test.tsx`, then rerun the full phase command set recorded in `17-VALIDATION.md`. Confirm the Library exposes editable curry compatibility, collapsed rows resolve live labels, and zero-compatible curries stay explicit across both expanded and collapsed states.
</verification>

<success_criteria>
Users can maintain curry compatibility directly in the Library using the established checklist pattern, the UI clearly distinguishes intentional zero-compatible curries from broken data, and Phase 17 has a concrete validation artifact covering both DB and UI behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/17-curry-compatibility-data/17-03-SUMMARY.md`
</output>
