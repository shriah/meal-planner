---
phase: 13-only-include-extras-when-explicitly-required
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/generator.ts
  - src/services/generator.test.ts
  - .planning/phases/13-only-include-extras-when-explicitly-required/13-VALIDATION.md
autonomous: true
requirements: [PH13-01, PH13-02, PH13-03, PH13-04]
must_haves:
  truths:
    - "Unlocked slots with no matching require-extra rule finish with an empty `extra_ids` array"
    - "A matching require-extra rule remains the only generator path that can add extras to an unlocked slot"
    - "Locked `extra_ids` remain authoritative and unchanged"
    - "Extra-related warnings still appear only when an explicit require-extra category cannot be satisfied"
  artifacts:
    - path: "src/services/generator.ts"
      provides: "Require-only extra generation path for unlocked slots"
    - path: "src/services/generator.test.ts"
      provides: "Regression coverage for no-extra-by-default, explicit require-extra success, locked extras, and unsatisfied warnings"
    - path: ".planning/phases/13-only-include-extras-when-explicitly-required/13-VALIDATION.md"
      provides: "Nyquist validation contract for the narrowed generator behavior"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 goal, requirement IDs, and executable plan listing"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceable PH13 requirement definitions"
  key_links:
    - from: "src/services/generator.ts"
      to: "src/services/generator.test.ts"
      via: "Regression coverage for default-empty extras, require-extra success, locked extras, and warning behavior"
      pattern: "require_extra|extra_ids|lockedSlots"
    - from: "src/services/generator.ts"
      to: "src/types/plan.ts"
      via: "RequireExtraEffect runtime handling"
      pattern: "RequireExtraEffect|kind === 'require_extra'"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 13 requirement ID traceability"
      pattern: "PH13-01|PH13-02|PH13-03|PH13-04"
---

<objective>
Bring runtime behavior in line with the Phase 13 product rule: unlocked slots must not receive generated extras unless a matching `require_extra` rule explicitly requires them, while locked extras and current unsatisfied-warning semantics remain intact.

Purpose: Remove the leftover fallback behavior from Phase 12 so generated plans become strictly rule-driven for extras.
Output: Updated generator/runtime behavior, aligned regression tests, and Phase 13 validation metadata.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-only-include-extras-when-explicitly-required/13-CONTEXT.md
@.planning/phases/13-only-include-extras-when-explicitly-required/13-RESEARCH.md
@.planning/phases/13-only-include-extras-when-explicitly-required/13-VALIDATION.md
@src/services/generator.ts
@src/services/generator.test.ts
@src/types/plan.ts
@src/services/food-db.ts

<interfaces>
From `src/types/plan.ts`:
```ts
export type RequireExtraEffect = Extract<AnyEffect, { kind: 'require_extra' }>;

export interface PlanSlot {
  day: DayOfWeek;
  meal_slot: MealSlot;
  base_id: number;
  curry_id?: number;
  subzi_id?: number;
  extra_ids: number[];
}
```

From `src/services/generator.ts`:
```ts
export interface GenerateOptions {
  lockedSlots?: Partial<Record<`${DayOfWeek}-${MealSlot}`, {
    base_id?: number;
    curry_id?: number;
    subzi_id?: number;
    extra_ids?: number[];
  }>>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove unlocked default extra fill while preserving explicit and locked-extra behavior</name>
  <files>src/services/generator.ts, src/services/generator.test.ts</files>
  <read_first>
    - src/services/generator.ts
    - src/services/generator.test.ts
    - src/types/plan.ts
    - src/services/food-db.ts
    - .planning/phases/13-only-include-extras-when-explicitly-required/13-CONTEXT.md
    - .planning/phases/13-only-include-extras-when-explicitly-required/13-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: per PH13-01, an unlocked slot with no matching `require_extra` effect returns `extra_ids: []`.
    - Test 2: per PH13-02, explicit `require_extra` remains the only runtime path that adds extras to an unlocked slot.
    - Test 3: per PH13-03, `lockedSlots[day-slot].extra_ids` is copied through unchanged.
    - Test 4: per PH13-04, extra-related warnings still appear only for unsatisfied explicit `require_extra` categories.
  </behavior>
  <action>
    In `src/services/generator.ts`, keep the locked `extra_ids` branch intact and keep the existing `require_extra` category collection and unsatisfied-warning path. Remove the post-requirement fallback loop that fills remaining extra capacity for unlocked slots, so `extra_quantity_limits` no longer cause optional extras to appear by themselves.

    In `src/services/generator.test.ts`, replace the outdated Phase 12 expectation that unlocked extras remain unconstrained without `require_extra`. Add or update focused regressions that prove default-empty unlocked extras, explicit `require_extra` success, preserved locked `extra_ids`, and unsatisfied-warning continuity. Do not add UI, persistence, or migration assertions in this task.
  </action>
  <acceptance_criteria>
    - `rg -n "without require_extra" src/services/generator.test.ts` finds a test that expects empty unlocked `extra_ids`, not unconstrained extras.
    - `rg -n "locked extra_ids|require_extra category" src/services/generator.test.ts` returns the preserved locked-extra and warning coverage.
    - `rg -n "remaining > 0|selectedExtraIds.length < maxExtras" src/services/generator.ts` no longer shows an unlocked random-fill loop.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/generator.test.ts</automated>
  </verify>
  <done>The generator only produces unlocked extras from explicit `require_extra` effects, while locked extras and unsatisfied warnings still behave as before.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Phase 13 validation contract after generator changes</name>
  <files>.planning/phases/13-only-include-extras-when-explicitly-required/13-VALIDATION.md</files>
  <read_first>
    - .planning/phases/13-only-include-extras-when-explicitly-required/13-VALIDATION.md
    - .planning/phases/13-only-include-extras-when-explicitly-required/13-CONTEXT.md
    - .planning/phases/13-only-include-extras-when-explicitly-required/13-RESEARCH.md
  </read_first>
  <action>
    In `.planning/phases/13-only-include-extras-when-explicitly-required/13-VALIDATION.md`, keep the focus generator-only: the quick command should remain `npx vitest run src/services/generator.test.ts`, the wave-0 bullets should explicitly cover the no-extra-by-default regression rewrite, and the sign-off stays draft/pending until execution proves the checks green. Finish by running the full suite so the validation contract names a real phase gate.
  </action>
  <acceptance_criteria>
    - `rg -n "PH13-01|PH13-02|PH13-03|PH13-04" .planning/phases/13-only-include-extras-when-explicitly-required/13-VALIDATION.md` returns the active requirement mapping.
    - `.planning/phases/13-only-include-extras-when-explicitly-required/13-VALIDATION.md` remains `status: draft`, `nyquist_compliant: false`, and `wave_0_complete: false` until execution completes.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Phase 13 has a draft validation contract that matches the runtime-only scope and final suite gate.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/services/generator.test.ts` after the runtime/test edit, then `npm test` before marking the phase complete. The phase only passes when unlocked slots default to empty extras without `require_extra`, locked extras remain exact, and extra warnings still originate only from unsatisfied explicit `require_extra` categories.
</verification>

<success_criteria>
PH13-01 through PH13-04 are covered by one atomic execution plan: no generated extras by default for unlocked slots, explicit require-extra still works, locked extra_ids remain authoritative, and the only extra-related warning path is the existing unsatisfied explicit requirement warning.
</success_criteria>

<output>
After completion, create `.planning/phases/13-only-include-extras-when-explicitly-required/13-01-SUMMARY.md`
</output>
