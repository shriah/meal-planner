---
phase: 1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes
plan: 03
type: execute
wave: 3
depends_on:
  - 1001-01
  - 1001-02
files_modified:
  - src/components/rules/ruleDescriptions.ts
  - src/components/rules/ruleDescriptions.test.ts
  - src/components/rules/RuleRow.tsx
  - src/components/rules/RuleRow.test.tsx
  - .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-VALIDATION.md
autonomous: true
requirements:
  - PH1001-RULE-LIST
must_haves:
  truths:
    - Saved combo rules render understandable summaries in the Rules list instead of raw ids or a generic rule label.
    - Users can still toggle, open, and edit combo rules from the same Rules list flow as the existing rule families.
    - The phase validation artifact points to the focused combo-rule test commands and the full gate needed to close the phase.
  artifacts:
    - path: src/components/rules/ruleDescriptions.ts
      provides: human-readable combo-rule summary generation
    - path: src/components/rules/RuleRow.tsx
      provides: list-row labeling and edit entrypoint for combo rules
    - path: .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-VALIDATION.md
      provides: Nyquist-compliant command map for all three plans
  key_links:
    - from: src/components/rules/RuleRow.tsx
      to: src/components/rules/EditRuleSheet.tsx
      via: edit action opening a combo-capable edit flow
      pattern: "EditRuleSheet"
    - from: src/components/rules/ruleDescriptions.ts
      to: src/types/plan.ts
      via: compiled-filter union formatting for combo rules
      pattern: "type.*combo|combo"
---

<objective>
Finish the user-facing rule-management surfaces for combo rules and align the phase validation contract to the shipped implementation.

Purpose: Implement PH1001-RULE-LIST so combo rules behave like first-class persisted rules in list summaries, edit entrypoints, and final verification.
Output: Readable combo rule descriptions in the Rules list, correct row labeling/edit wiring, and a finalized phase validation artifact with focused commands plus the full phase gate.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@/Users/harish/workspace/food-planner/node_modules/next/dist/docs/index.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-CONTEXT.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-RESEARCH.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-VALIDATION.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-01-PLAN.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-02-PLAN.md
@src/components/rules/ruleDescriptions.ts
@src/components/rules/RuleRow.tsx
@src/components/rules/EditRuleSheet.tsx

<interfaces>
From src/components/rules/ruleDescriptions.ts:
```typescript
export function describeRule(rule: CompiledRule, categories: CategoryRecord[] = []): string;
```

From src/components/rules/RuleRow.tsx:
```typescript
interface RuleRowProps {
  rule: RuleRecord
}
```

From src/components/rules/EditRuleSheet.tsx:
```typescript
export function EditRuleSheet({
  rule,
  open,
  onOpenChange,
}: EditRuleSheetProps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render combo rules as first-class entries in rule summaries and row chrome</name>
  <files>src/components/rules/ruleDescriptions.ts, src/components/rules/ruleDescriptions.test.ts, src/components/rules/RuleRow.tsx, src/components/rules/RuleRow.test.tsx</files>
  <read_first>AGENTS.md, .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-CONTEXT.md, src/components/rules/ruleDescriptions.ts, src/components/rules/ruleDescriptions.test.ts, src/components/rules/RuleRow.tsx, src/components/rules/RuleRow.test.tsx</read_first>
  <behavior>
    - Test 1: combo rules describe their base target plus exact named companions in readable summary copy instead of raw ids.
    - Test 2: deleted companion ids fall back to explicit “deleted component” copy rather than empty strings or unrelated substitutions.
    - Test 3: Rule rows show an explicit combo label and still open the shared edit sheet for combo rules.
  </behavior>
  <action>Extend the rule description helpers and row label mapping to handle the combo-rule union introduced in Plan 01. Resolve base-category labels from live category records and component labels from the current component library where available; when a referenced combo companion is gone, render deleted-component fallback copy instead of raw ids or silence. Update row tests so combo rules appear as their own rule family in badges/labels and still reuse the common toggle, edit, and delete flow. Do not collapse combo summaries back into generic “require one” or “skip component” phrasing.</action>
  <verify>
    <automated>npm test -- src/components/rules/ruleDescriptions.test.ts src/components/rules/RuleRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>Combo rules have readable summaries and list-row labels, deleted references are explained without raw ids, and the shared row edit flow still works for both combo and generic rules.</acceptance_criteria>
  <done>The Rules list treats combo rules as a real rule family instead of an opaque persisted payload.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize the combo-rule validation contract and run the phase gate</name>
  <files>.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-VALIDATION.md</files>
  <read_first>.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-VALIDATION.md, .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-01-PLAN.md, .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-02-PLAN.md, .planning/ROADMAP.md</read_first>
  <behavior>
    - Validation maps PH1001-COMPILER, PH1001-FORM, PH1001-GENERATOR, PH1001-MANUAL, and PH1001-RULE-LIST to the exact focused commands used by the phase.
    - Wave and task ids match the shipped plan/task structure without stale placeholders.
    - The phase gate requires `npm test` as the final completion condition.
  </behavior>
  <action>Update `1001-VALIDATION.md` so its task table, per-wave sampling commands, and sign-off checklist reflect the actual file/test layout shipped by Plans 01 through 03. Set `nyquist_compliant: true` only after the task map is complete and every plan task has an automated command. Keep the final gate as `npm test`, and do not leave placeholder requirement names or stale task ids behind.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>The validation artifact matches the real combo-rule implementation, every requirement has a focused automated proof, and the full test suite passes as the closing gate.</acceptance_criteria>
  <done>The phase has a current validation contract and one authoritative full-suite gate for closure.</done>
</task>

</tasks>

<verification>
Run the focused rule-list tests before updating the validation artifact. The plan closes only after `1001-VALIDATION.md` matches the shipped tasks and the full `npm test` gate passes.
</verification>

<success_criteria>
Combo rules are understandable and editable from the Rules list, and the phase closes with a current validation contract plus a green full-suite gate.
</success_criteria>

<output>
After completion, create `.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-03-SUMMARY.md`
</output>
