---
phase: 1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/plan.ts
  - src/db/client.ts
  - src/services/rule-compiler.ts
  - src/services/rule-compiler.test.ts
  - src/components/rules/types.ts
  - src/components/rules/form-state.ts
  - src/components/rules/form-state.test.ts
  - src/components/rules/RuleForm.tsx
  - src/components/rules/EditRuleSheet.tsx
  - src/components/rules/RuleFormFields/RuleFields.tsx
  - src/components/rules/RuleFormFields/RuleFields.test.tsx
  - src/components/rules/RuleForm.test.tsx
autonomous: true
requirements:
  - PH1001-COMPILER
  - PH1001-FORM
must_haves:
  truths:
    - Users can start a dedicated combo rule instead of trying to encode exact companions through the generic rule effects model.
    - A combo rule can target either one exact base component or one base category and save exact curry, subzi, and extra companions.
    - Editing an existing combo rule rehydrates its saved target and companion selections without mutating existing scheduling-rule or meal-template behavior.
  artifacts:
    - path: src/types/plan.ts
      provides: Compiled combo-rule schema and widened compiled-filter union
    - path: src/services/rule-compiler.ts
      provides: Combo compile/decompile helpers separate from the generic effects-based rule compiler
    - path: src/components/rules/RuleForm.tsx
      provides: Create flow that can save either the existing rule family or a combo rule
    - path: src/components/rules/EditRuleSheet.tsx
      provides: Edit flow that hydrates and saves combo rules
  key_links:
    - from: src/components/rules/RuleForm.tsx
      to: src/services/rule-compiler.ts
      via: save handler choosing combo compile path vs existing rule compile path
      pattern: "compile.*Rule|compile.*Combo"
    - from: src/components/rules/EditRuleSheet.tsx
      to: src/services/rule-compiler.ts
      via: decompile before edit and compile on save
      pattern: "decompile.*Rule|compile.*Rule"
    - from: src/db/client.ts
      to: src/types/plan.ts
      via: RuleRecord compiled_filter union typing
      pattern: "compiled_filter"
---

<objective>
Introduce the dedicated combo-rule contract and creation/editing flow without overloading the existing scheduling-rule and meal-template semantics.

Purpose: Implement D-01 through D-09 at the persistence, compiler, and form boundaries so later generator work can consume one explicit combo-rule shape instead of ad hoc effects.
Output: A widened compiled-filter union, dedicated combo compile/decompile helpers, and Rules create/edit flows that can author exact base-linked companion bundles.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@/Users/harish/workspace/food-planner/node_modules/next/dist/docs/index.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-CONTEXT.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-RESEARCH.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-VALIDATION.md
@.planning/milestones/v1.3-phases/19-explicit-override-paths/19-01-SUMMARY.md
@src/types/plan.ts
@src/db/client.ts
@src/services/rule-compiler.ts
@src/components/rules/types.ts
@src/components/rules/form-state.ts
@src/components/rules/RuleForm.tsx
@src/components/rules/EditRuleSheet.tsx
@src/components/rules/RuleFormFields/RuleFields.tsx

<interfaces>
From src/types/plan.ts:
```typescript
export const TargetSchema = z.discriminatedUnion('mode', [
  z.object({ mode: z.literal('component_type'), component_type: z.enum(['base', 'curry', 'subzi']) }),
  z.object({ mode: z.literal('tag'), filter: TagFilterSchema }),
  z.object({ mode: z.literal('component'), component_id: z.number() }),
  z.object({ mode: z.literal('base_category'), category_id: z.number() }),
]);

export const CompiledRuleSchema = z.object({
  type: z.literal('rule'),
  target: TargetSchema,
  scope: RuleScopeSchema,
  effects: z.array(EffectSchema),
});
```

From src/services/rule-compiler.ts:
```typescript
export function compileRule(state: RuleFormState): CompiledRule;
export function decompileRule(
  compiled: CompiledRule,
  name: string,
  context?: DecompileCategoryContext,
): RuleFormState;
```

From src/components/rules/types.ts:
```typescript
export type RuleFormState = {
  name: string;
  target: TargetFormState;
  days: DayOfWeek[];
  slots: MealSlot[];
  selection: 'filter_pool' | 'require_one' | 'exclude' | 'no_repeat' | '';
  allowed_slots: MealSlot[];
  skip_component_types: ('curry' | 'subzi')[];
  require_extra_category_ids: number[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the dedicated combo-rule union and reversible compiler boundary</name>
  <files>src/types/plan.ts, src/db/client.ts, src/services/rule-compiler.ts, src/services/rule-compiler.test.ts, src/components/rules/types.ts, src/components/rules/form-state.ts, src/components/rules/form-state.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-CONTEXT.md, .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-RESEARCH.md, src/types/plan.ts, src/db/client.ts, src/services/rule-compiler.ts, src/components/rules/types.ts, src/components/rules/form-state.ts</read_first>
  <behavior>
    - Test 1: combo rules serialize as a dedicated compiled-filter variant with base-component or base-category targeting only, per D-01, D-03, and D-06.
    - Test 2: compile/decompile round-trips exact curry, subzi, and extra component ids without mapping them onto generic selection effects, per D-02, D-07, and D-08.
    - Test 3: edit hydration drops deleted or missing companion ids instead of inventing fallback components, while preserving the rest of the combo state for resave.
  </behavior>
  <action>Widen the persisted rule shape from `CompiledRule` to a discriminated `CompiledFilter` union in `src/types/plan.ts` and `src/db/client.ts`. Add a dedicated combo-rule schema whose target is limited to either one base component or one base category per D-03 and whose payload stores exact companion ids for curry, subzi, and extras per D-06 through D-08; do not model combos as more `effects`, tags, or category requirements per D-01 and D-02. Update `src/services/rule-compiler.ts` so generic rules still use the current compile/decompile path unchanged, while combo rules get their own reversible helpers. Extend rule form state/types only where needed to represent the combo family cleanly; do not fold combo semantics into the generic reducer vocabulary for filter/exclude/require-one behavior. Lock the contract with focused compiler and state tests.</action>
  <verify>
    <automated>npm test -- src/services/rule-compiler.test.ts src/components/rules/form-state.test.ts</automated>
  </verify>
  <acceptance_criteria>The codebase has a typed combo-rule union, RuleRecord can persist it in the existing rules table, and compile/decompile tests prove exact companion ids round-trip without reusing generic rule effects.</acceptance_criteria>
  <done>The persistence and compiler boundary now exposes one dedicated combo-rule family that downstream UI and generator code can target directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add combo create and edit flows inside the existing Rules shell</name>
  <files>src/components/rules/RuleForm.tsx, src/components/rules/EditRuleSheet.tsx, src/components/rules/RuleFormFields/RuleFields.tsx, src/components/rules/RuleFormFields/RuleFields.test.tsx, src/components/rules/RuleForm.test.tsx, src/components/rules/types.ts, src/components/rules/form-state.ts</files>
  <read_first>AGENTS.md, /Users/harish/workspace/food-planner/node_modules/next/dist/docs/index.md, .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-CONTEXT.md, src/components/rules/RuleForm.tsx, src/components/rules/EditRuleSheet.tsx, src/components/rules/RuleFormFields/RuleFields.tsx, src/components/rules/RuleForm.test.tsx, src/components/rules/RuleFormFields/RuleFields.test.tsx</read_first>
  <behavior>
    - Test 1: users can switch into combo-rule authoring, choose either a specific base component or a base category, and select exact named companions for curry, subzi, and extras.
    - Test 2: combo rules require at least one explicit companion slot and never expose generic selection, tag-companion, or category-companion controls, per D-02, D-04, and D-08.
    - Test 3: editing a saved combo rule rehydrates its exact companion selections and preserves existing scheduling-rule editing behavior.
  </behavior>
  <action>Extend the create page and edit sheet so the user can choose between the existing generic rule family and the new combo-rule family. Reuse the existing shell layout, save buttons, and live category/component data, but add a dedicated combo authoring section that supports exact base-component or base-category targeting per D-03 and exact companion picks per D-06 through D-08. Require at least one companion slot before save and keep extras as an exact component list per D-07. Preserve the current generic RuleFields flow for scheduling and meal-template behavior; do not blur the two families together, do not add broad composition-mode controls from deferred Phase 999.1, and do not let combo UI imply tag-based or category-based companion matching.</action>
  <verify>
    <automated>npm test -- src/components/rules/RuleForm.test.tsx src/components/rules/RuleFormFields/RuleFields.test.tsx</automated>
  </verify>
  <acceptance_criteria>The Rules create/edit surfaces can save and rehydrate combo rules with exact named companions, while the existing generic rule authoring flow remains intact and separately scoped.</acceptance_criteria>
  <done>Users can author combo rules through the shipped Rules UI without approximating them through the old rule vocabulary.</done>
</task>

</tasks>

<verification>
Run the focused compiler and form suites after each task. Before closing the plan, verify the create and edit flows both call the correct compile/decompile branch for combo rules and that no combo companion selector accepts tags or categories.
</verification>

<success_criteria>
The project has one dedicated persisted combo-rule contract and the Rules create/edit surfaces can round-trip exact base-linked companion bundles through it.
</success_criteria>

<output>
After completion, create `.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-01-SUMMARY.md`
</output>
