---
phase: 1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes
plan: 02
type: execute
wave: 2
depends_on:
  - 1001-01
files_modified:
  - src/services/generator.ts
  - src/services/generator.test.ts
  - src/db/client.ts
  - src/services/plan-db.ts
  - src/stores/plan-store.ts
  - src/stores/plan-store.test.ts
autonomous: true
requirements:
  - PH1001-GENERATOR
  - PH1001-MANUAL
must_haves:
  truths:
    - Automatic generation picks the winning combo for a slot immediately after base selection and applies any valid exact companions named by that combo.
    - A base-component combo wins over a matching base-category combo for the same slot, and unspecified companion slots still fall back to the normal generator path.
    - Manual curry, subzi, and extra selections remain authoritative on regenerate instead of being overwritten by a combo rule.
  artifacts:
    - path: src/services/generator.ts
      provides: combo resolution, application, and broken-reference warning logic
    - path: src/stores/plan-store.ts
      provides: persisted manual companion ownership forwarded through regenerate
    - path: src/services/plan-db.ts
      provides: storage seam for any new manual-companion ownership metadata
  key_links:
    - from: src/services/generator.ts
      to: src/db/client.ts
      via: validated compiled_filter union including combo rules
      pattern: "compiled_filter|Combo"
    - from: src/stores/plan-store.ts
      to: src/services/generator.ts
      via: regenerate passing locked and manual-owned companion constraints
      pattern: "generate\\("
    - from: src/services/plan-db.ts
      to: src/stores/plan-store.ts
      via: active plan persistence for manual companion ownership
      pattern: "saveActivePlan|saveWeekPlan"
---

<objective>
Apply combo rules at generation time and preserve explicit manual companion intent so combo-selected bundles are strong defaults, not user-hostile overrides.

Purpose: Implement D-05 and D-09 through D-15 at the generator and plan-store seams while preserving the existing explicit-intent precedence established in Phases 19 and 21.
Output: A generator that resolves and applies the winning combo rule with partial-warning degradation, plus store persistence that keeps manual companion picks above combo behavior on regenerate.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-CONTEXT.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-RESEARCH.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-VALIDATION.md
@.planning/milestones/v1.3-phases/19-explicit-override-paths/19-02-SUMMARY.md
@.planning/milestones/v1.3-phases/21-wire-planboard-curry-override-flow/21-01-SUMMARY.md
@.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-01-PLAN.md
@src/services/generator.ts
@src/stores/plan-store.ts
@src/services/plan-db.ts
@src/db/client.ts

<interfaces>
From src/services/generator.ts:
```typescript
export interface GenerateOptions {
  lockedSlots?: Partial<Record<`${DayOfWeek}-${MealSlot}`, {
    base_id?: number;
    curry_id?: number;
    subzi_id?: number;
    extra_ids?: number[];
  }>>;
}

interface ValidatedRule {
  compiled: CompiledRule;
  id: number;
}
```

From src/stores/plan-store.ts:
```typescript
swapComponent: (
  day: DayOfWeek,
  slot: MealSlot,
  componentType: 'base' | 'curry' | 'subzi' | 'extras',
  componentId: number,
) => void

regenerate: () => Promise<void>
```

From src/services/plan-db.ts:
```typescript
export async function saveActivePlan(data: { plan: WeeklyPlan; locks: Record<string, boolean> }): Promise<void>;
export async function saveWeekPlan(weekStart: string, plan: WeeklyPlan, locks: Record<string, boolean>): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve and apply the winning combo rule inside the generator</name>
  <files>src/services/generator.ts, src/services/generator.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-CONTEXT.md, .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-RESEARCH.md, src/services/generator.ts, src/services/generator.test.ts, src/types/plan.ts</read_first>
  <behavior>
    - Test 1: when both a base-component combo and a base-category combo match the same slot, the base-component combo wins, per D-05.
    - Test 2: combo-selected curry, subzi, and extras are treated as authoritative for the explicitly named companion slots and are not later filtered, replaced, or reshaped by normal rule helpers, per D-09 through D-11.
    - Test 3: if one or more referenced combo companions are missing, the generator applies the remaining valid companions and emits a warning instead of substituting unrelated components, per D-15.
  </behavior>
  <action>After base selection and before the normal curry/subzi/extra selection path, scan the validated compiled-filter union for combo rules that match the slot scope and selected base. Implement winning-combo precedence so an exact base-component target beats a matching base-category target per D-05. Apply exact named companions for only the slots present in the combo payload per D-08, and mark those companion slots as combo-owned so later generic rule helpers, compatibility filters, and extra requirement flows cannot rewrite them per D-09 through D-11. Unspecified companion slots must continue through the current generator logic. For missing or deleted companion ids, keep the valid parts of the bundle, emit the existing slot-scoped warning shape with the combo rule id, and never substitute fallback components per D-15.</action>
  <verify>
    <automated>npm test -- src/services/generator.test.ts</automated>
  </verify>
  <acceptance_criteria>The generator can select the winning combo by base specificity, apply its exact valid companions without downstream rewrites, and warn while partially degrading when referenced companions are missing.</acceptance_criteria>
  <done>Automatic generation now has a real combo-rule runtime contract instead of treating exact companions as a UI-only rule artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve manual companion ownership above combo rules on regenerate</name>
  <files>src/db/client.ts, src/services/plan-db.ts, src/stores/plan-store.ts, src/stores/plan-store.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-CONTEXT.md, .planning/milestones/v1.3-phases/19-explicit-override-paths/19-CONTEXT.md, src/db/client.ts, src/services/plan-db.ts, src/stores/plan-store.ts, src/stores/plan-store.test.ts</read_first>
  <behavior>
    - Test 1: manually swapped curry, subzi, and extras are persisted as explicit user-owned choices even when the slot is not separately locked.
    - Test 2: regenerate forwards those user-owned companion ids back into generation so combo rules cannot overwrite them, per D-12 through D-14.
    - Test 3: existing lock behavior continues to work, and base-only regeneration still allows combo rules to fill untouched companion slots normally.
  </behavior>
  <action>Extend the active-plan persistence seam so manual companion selections can be remembered independently from the existing boolean lock map; keep the lock behavior intact, but add the minimum persisted metadata needed for curry/subzi/extras manual ownership per D-12 and D-13. Update `swapComponent` so explicit companion picks are marked as user-owned, and update `regenerate` so those owned ids are forwarded back into `generate(...)` as authoritative slot constraints even when the user did not toggle the lock control. Scope this to companion components for this phase; do not invent a new user-facing control, do not auto-lock rows as a side effect, and do not let combo rules overwrite user-owned companions on normal regenerate.</action>
  <verify>
    <automated>npm test -- src/stores/plan-store.test.ts</automated>
  </verify>
  <acceptance_criteria>Manual curry, subzi, and extras selections survive persistence and regenerate as explicit user intent, while untouched companion slots remain available for combo-driven generation.</acceptance_criteria>
  <done>Manual companion picks now outrank combo rules in the actual regenerate path, not just in product copy.</done>
</task>

</tasks>

<verification>
Run the focused generator and store suites after each task. Before closing the plan, verify that a slot with a manual curry but no manual subzi still keeps the manual curry while a combo rule can populate only the remaining open companion slots.
</verification>

<success_criteria>
The winning combo rule is applied during automatic generation with correct precedence and broken-reference warnings, while explicit manual companion picks still survive regenerate as the strongest slot-level intent.
</success_criteria>

<output>
After completion, create `.planning/phases/1001-add-base-linked-meal-combo-rules-for-exact-companion-dishes/1001-02-SUMMARY.md`
</output>
