---
phase: 12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/services/rule-compiler.ts
  - src/services/rule-compiler.test.ts
  - src/components/rules/ruleDescriptions.ts
  - src/components/rules/ruleDescriptions.test.ts
  - src/db/client.ts
  - src/db/migrations.test.ts
autonomous: true
requirements: [PH12-02, PH12-03, PH12-05]
must_haves:
  truths:
    - "Saving or rehydrating meal-template rules never recreates legacy exclude-extra behavior"
    - "Existing stored rules are normalized so exclude-extra data is stripped during migration"
    - "User-visible rule descriptions mention only required extras"
  artifacts:
    - path: "src/services/rule-compiler.ts"
      provides: "Compiler/decompiler symmetry without exclude-extra round-tripping"
    - path: "src/components/rules/ruleDescriptions.ts"
      provides: "Required-extra-only user-facing copy"
    - path: "src/db/client.ts"
      provides: "Dexie v10 migration and strip helper for legacy exclude-extra cleanup"
    - path: "src/db/migrations.test.ts"
      provides: "Migration coverage for stripping legacy exclude-extra while retaining require-extra"
  key_links:
    - from: "src/services/rule-compiler.ts"
      to: "src/components/rules/types.ts"
      via: "RuleFormState compile/decompile contract"
      pattern: "require_extra_categories"
    - from: "src/components/rules/ruleDescriptions.ts"
      to: "src/services/rule-compiler.ts"
      via: "Description copy must match normalized compiled rule semantics"
      pattern: "require_extra"
    - from: "src/db/client.ts"
      to: "src/services/rule-compiler.ts"
      via: "Migration-first cleanup prevents save paths from preserving legacy effects"
      pattern: "stripLegacyExcludeExtra|exclude_extra"
---

<objective>
Normalize compiler, description, and persistence behavior so exclude-extra cannot survive create/edit round-trips or stored data upgrades. This plan isolates the semantics and migration boundary from both UI work and generator runtime cleanup.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-CONTEXT.md
@.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-RESEARCH.md
@.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md
@.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove exclude-extra from compiler round-trips and rule descriptions</name>
  <files>src/services/rule-compiler.ts, src/services/rule-compiler.test.ts, src/components/rules/ruleDescriptions.ts, src/components/rules/ruleDescriptions.test.ts</files>
  <read_first>
    - src/services/rule-compiler.ts
    - src/services/rule-compiler.test.ts
    - src/components/rules/ruleDescriptions.ts
    - src/components/rules/ruleDescriptions.test.ts
    - src/components/rules/types.ts
    - .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-CONTEXT.md
    - .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `compileRule()` never emits `{ kind: 'exclude_extra' }`.
    - Test 2: `decompileRule()` ignores legacy `exclude_extra` input rather than hydrating hidden form state.
    - Test 3: `describeRule()` never renders exclusion language for extras and still renders required-extra copy.
  </behavior>
  <action>
    Update `src/services/rule-compiler.ts` so `compileRule(state)` pushes only `allowed_slots`, `skip_component`, and `require_extra` composition effects. Update `decompileRule(compiled, name)` so it initializes only `require_extra_categories` for extra behavior and ignores incoming `effect.kind === 'exclude_extra'`.

    Update `src/services/rule-compiler.test.ts` to prove meal-template round-trips contain only the supported effect families and that legacy `exclude_extra` input is dropped on decompile/compile.

    Update `src/components/rules/ruleDescriptions.ts` and `src/components/rules/ruleDescriptions.test.ts` to remove the exclude-extra branch and keep only required-extra copy.
  </action>
  <acceptance_criteria>
    - `rg -n "kind: 'exclude_extra'" src/services/rule-compiler.ts src/components/rules/ruleDescriptions.ts` returns no matches.
    - `npx vitest run src/services/rule-compiler.test.ts src/components/rules/ruleDescriptions.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/rule-compiler.test.ts src/components/rules/ruleDescriptions.test.ts</automated>
  </verify>
  <done>Compiler round-trips and descriptions reflect only the supported require-extra semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip legacy exclude-extra data in migration and normalization paths</name>
  <files>src/db/client.ts, src/db/migrations.test.ts</files>
  <read_first>
    - src/db/client.ts
    - src/db/migrations.test.ts
    - src/services/rule-compiler.ts
    - src/types/plan.ts
    - .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-CONTEXT.md
    - .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Legacy meal-template objects with `exclude_extra_categories` no longer emit compiled `exclude_extra` effects.
    - Test 2: A pure helper strips `{ kind: 'exclude_extra' }` from already-compiled stored rules without altering `require_extra`.
    - Test 3: Dexie adds a new schema version after v9 that applies the strip helper to every stored rule record during upgrade.
  </behavior>
  <action>
    In `src/db/client.ts`, add and export `stripLegacyExcludeExtra(cf: unknown): unknown`. It should return unknown/non-rule shapes unchanged and, for compiled `type: 'rule'` records, filter `effects` so every `{ kind: 'exclude_extra' }` entry is removed while all other effect objects remain intact.

    Update `migrateToCompiledRule(cf)` so the legacy `meal-template` branch never emits `{ kind: 'exclude_extra' }`, even if `exclude_extra_categories` exists on the input record. Retain `allowed_slots`, `skip_component`, and `require_extra`.

    Add `db.version(10)` with the same stores map as v9 and an upgrade block that rewrites every stored rule via `stripLegacyExcludeExtra`.

    Update `src/db/migrations.test.ts` to cover legacy meal-template input, already-compiled rule input, and pass-through behavior for unrelated shapes.
  </action>
  <acceptance_criteria>
    - `rg -n "export function stripLegacyExcludeExtra|db\\.version\\(10\\)|rule\\.compiled_filter = stripLegacyExcludeExtra" src/db/client.ts` returns all required patterns.
    - `rg -n "exclude_extra_categories" src/db/client.ts` returns matches only in legacy input handling, not in emitted effect objects.
    - `npx vitest run src/db/migrations.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/db/migrations.test.ts</automated>
  </verify>
  <done>Legacy exclude-extra data is removed at the migration boundary so stored rules match the new product model.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/services/rule-compiler.test.ts src/components/rules/ruleDescriptions.test.ts src/db/migrations.test.ts`.
</verification>

<success_criteria>
PH12-02, PH12-03, and PH12-05 are satisfied: exclude-extra cannot survive compiler/decompiler flows, user-visible copy is normalized, and persisted rule cleanup is versioned and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-02-SUMMARY.md`
</output>
