---
phase: 1000-remove-the-compatability-base-for-extras
plan: 02
type: execute
wave: 2
depends_on:
  - 1000-01
files_modified:
  - src/components/library/ComponentForm.tsx
  - src/components/library/ComponentRow.tsx
  - src/components/library/ComponentForm.test.tsx
  - src/components/library/ComponentRow.test.tsx
  - src/components/plan/MealPickerSheet.tsx
  - src/components/plan/MealPickerSheet.test.tsx
autonomous: true
requirements:
  - PH1000-01
  - PH1000-02
must_haves:
  truths:
    - Library create/edit flows for extras no longer show or imply base compatibility controls.
    - Collapsed extra rows summarize only still-real metadata, not removed compatibility labels.
    - Manual extra picking shows the full extra library instead of shrinking with the selected base.
  artifacts:
    - path: src/components/library/ComponentForm.tsx
      provides: extra form without compatibility checklist UI
    - path: src/components/library/ComponentRow.tsx
      provides: extra row summary without compatibility badges
    - path: src/components/plan/MealPickerSheet.tsx
      provides: flat extra picker query path
  key_links:
    - from: src/components/library/ComponentForm.tsx
      to: src/services/food-db.ts
      via: add/update component payloads
      pattern: "componentType === 'extra'"
    - from: src/components/plan/MealPickerSheet.tsx
      to: src/services/food-db.ts
      via: useLiveQuery extra loading
      pattern: "getComponentsByType\\('extra'\\)"
---

<objective>
Remove the extra-specific compatibility UI and picker filtering so the product story matches the simplified data contract: extras are globally pickable and not base-bound.

Purpose: Implement D-03 and D-06 directly in the library and picker seams while preserving curry-specific compatibility UI/grouping per D-10.
Output: Extra forms and rows without compatibility affordances, plus a meal picker that loads all extras regardless of the current base context.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@/Users/harish/workspace/food-planner/node_modules/next/dist/docs/01-app/index.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1000-remove-the-compatability-base-for-extras/1000-CONTEXT.md
@.planning/phases/1000-remove-the-compatability-base-for-extras/1000-RESEARCH.md
@.planning/phases/1000-remove-the-compatability-base-for-extras/1000-01-PLAN.md
@.planning/phases/20-compatibility-regression-coverage/20-02-SUMMARY.md
@src/components/library/ComponentForm.tsx
@src/components/library/ComponentRow.tsx
@src/components/plan/MealPickerSheet.tsx
@src/components/library/ComponentForm.test.tsx
@src/components/library/ComponentRow.test.tsx
@src/components/plan/MealPickerSheet.test.tsx

<interfaces>
From src/components/plan/MealPickerSheet.tsx:
```typescript
interface MealPickerSheetProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  day: DayOfWeek;
  slot: MealSlot;
  componentType: 'base' | 'curry' | 'subzi' | 'extras';
  currentBaseCategoryId?: number;
}
```

From src/components/library/ComponentForm.tsx:
```typescript
interface FormState {
  name: string;
  base_category_id: string;
  extra_category_id: string;
  compatible_base_category_ids: number[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete extra compatibility editing and summary UI from the Library</name>
  <files>src/components/library/ComponentForm.tsx, src/components/library/ComponentRow.tsx, src/components/library/ComponentForm.test.tsx, src/components/library/ComponentRow.test.tsx</files>
  <read_first>AGENTS.md, /Users/harish/workspace/food-planner/node_modules/next/dist/docs/01-app/index.md, .planning/phases/1000-remove-the-compatability-base-for-extras/1000-CONTEXT.md, src/components/library/ComponentForm.tsx, src/components/library/ComponentRow.tsx, src/components/library/ComponentForm.test.tsx, src/components/library/ComponentRow.test.tsx</read_first>
  <behavior>
    - Test 1: Extra create/edit flows only show extra-category selection and shared tags, not compatible-base checklists.
    - Test 2: Saving an extra writes no compatibility arrays.
    - Test 3: Collapsed extra rows show extra-category metadata only, while curry summaries/warnings remain intact.
  </behavior>
  <action>Remove the extra-only compatibility checklist, payload mapping, and collapsed-row summary badges from the library per D-03. Keep curry compatibility editing, zero-compatible warnings, and row summaries unchanged per D-10. Rewrite the library tests so they assert the new extra-only form contract and verify the row UI no longer implies extras are scoped to bases.</action>
  <verify>
    <automated>npm test -- src/components/library/ComponentForm.test.tsx src/components/library/ComponentRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>Extras can still be created and edited, but the Library exposes no extra/base compatibility controls or summary badges, and curry-specific compatibility UI still passes unchanged tests.</acceptance_criteria>
  <done>The Library no longer teaches or persists a removed extra/base compatibility concept.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the manual extra picker load all extras without base filtering</name>
  <files>src/components/plan/MealPickerSheet.tsx, src/components/plan/MealPickerSheet.test.tsx</files>
  <read_first>AGENTS.md, /Users/harish/workspace/food-planner/node_modules/next/dist/docs/01-app/index.md, .planning/phases/1000-remove-the-compatability-base-for-extras/1000-RESEARCH.md, src/components/plan/MealPickerSheet.tsx, src/components/plan/MealPickerSheet.test.tsx, src/services/food-db.ts</read_first>
  <behavior>
    - Test 1: Opening the extras picker uses the flat extra query path even when a base category is present.
    - Test 2: Extra picker results stay searchable/filterable by tags, but not by selected base.
    - Test 3: Curry picker grouping still uses base context and is not regressed by the extras cleanup.
  </behavior>
  <action>Refactor `MealPickerSheet` so `componentType === 'extras'` always loads `getComponentsByType('extra')` per D-06, with no empty-state branch tied to `currentBaseCategoryId`. Keep curry grouping and the rest of the picker modes unchanged per D-10. Rewrite picker tests so they lock in the flat extra query behavior and preserve the existing curry override grouping assertions.</action>
  <verify>
    <automated>npm test -- src/components/plan/MealPickerSheet.test.tsx src/components/plan/PlanBoard.test.tsx</automated>
  </verify>
  <acceptance_criteria>The manual extra picker returns all extras regardless of the selected base, while the curry picker still classifies compatible and override choices using base context.</acceptance_criteria>
  <done>Manual extra selection is globally available and no hidden base filter remains in the picker seam.</done>
</task>

</tasks>

<verification>
Focused UI verification is fully automated. The plan is complete when the library and picker test suites both prove extras are no longer shown or loaded as base-scoped data.
</verification>

<success_criteria>
Users no longer see extra/base compatibility UI in the Library, and the extra picker shows the full extra library regardless of current base selection.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-remove-the-compatability-base-for-extras/1000-02-SUMMARY.md`
</output>
