---
phase: 1000-remove-the-compatability-base-for-extras
plan: 03
type: execute
wave: 3
depends_on:
  - 1000-01
  - 1000-02
files_modified:
  - src/services/generator.ts
  - src/services/generator.test.ts
  - src/services/curry-compatibility-regression.test.ts
  - .planning/phases/1000-remove-the-compatability-base-for-extras/1000-VALIDATION.md
autonomous: true
requirements:
  - PH1000-03
  - PH1000-04
  - PH1000-06
must_haves:
  truths:
    - Automatic generation only adds extras when an explicit `require_extra` rule matches the slot.
    - If no rule requires an extra, generated slots keep `extra_ids: []` and no fallback auto-fill returns.
    - Removing extra/base compatibility does not weaken or alter the existing curry compatibility contract.
  artifacts:
    - path: src/services/generator.ts
      provides: extra selection logic without base-compatibility gating or fallback fill
    - path: src/services/generator.test.ts
      provides: generator regressions for require-extra-only extra behavior
    - path: .planning/phases/1000-remove-the-compatability-base-for-extras/1000-VALIDATION.md
      provides: Nyquist-compliant command map for the phase
  key_links:
    - from: src/services/generator.ts
      to: src/types/plan.ts
      via: require_extra effect handling
      pattern: "kind === 'require_extra'"
    - from: src/services/generator.ts
      to: src/services/curry-compatibility-regression.test.ts
      via: unchanged curry compatibility contract
      pattern: "compatible_base_category_ids"
---

<objective>
Finalize runtime behavior so extras stay explicit-only after compatibility removal, and lock the phase with regressions plus a validation contract that also proves curry behavior did not drift.

Purpose: Implement D-04, D-05, D-07, and D-10 in the generator seam, then close the phase with automated proof.
Output: A generator that never base-filters or fallback-fills extras, regression coverage for the final runtime contract, and an approved-ready validation artifact.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1000-remove-the-compatability-base-for-extras/1000-CONTEXT.md
@.planning/phases/1000-remove-the-compatability-base-for-extras/1000-RESEARCH.md
@.planning/phases/1000-remove-the-compatability-base-for-extras/1000-VALIDATION.md
@.planning/phases/1000-remove-the-compatability-base-for-extras/1000-01-PLAN.md
@.planning/phases/1000-remove-the-compatability-base-for-extras/1000-02-PLAN.md
@.planning/milestones/v1.2-phases/13-only-include-extras-when-explicitly-required/13-01-SUMMARY.md
@.planning/phases/20-compatibility-regression-coverage/20-01-SUMMARY.md
@src/services/generator.ts
@src/services/generator.test.ts
@src/services/curry-compatibility-regression.test.ts

<interfaces>
From src/services/generator.ts:
```typescript
export interface GenerateOptions {
  lockedSlots?: Partial<Record<`${DayOfWeek}-${MealSlot}`, {
    base_id?: number;
    curry_id?: number;
    subzi_id?: number;
    extra_ids?: number[];
  }>>;
}

export async function generate(options?: GenerateOptions): Promise<GeneratorResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove base-compatibility gating from extra generation while preserving explicit-only rules</name>
  <files>src/services/generator.ts, src/services/generator.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/1000-remove-the-compatability-base-for-extras/1000-CONTEXT.md, .planning/milestones/v1.2-phases/13-only-include-extras-when-explicitly-required/13-01-SUMMARY.md, src/services/generator.ts, src/services/generator.test.ts, src/types/plan.ts</read_first>
  <behavior>
    - Test 1: Without `require_extra`, unlocked slots still finish with `extra_ids: []`.
    - Test 2: When `require_extra` targets a matching extra category, any eligible extra in that category may be added regardless of base.
    - Test 3: If no eligible extra exists for a required category, the existing warning path remains and no fallback auto-fill appears.
  </behavior>
  <action>Delete the extra/base compatibility helper and any selection code that narrows extra candidates by the chosen base per D-01, D-04, D-05, and D-07. Keep the Phase 13 explicit-only contract intact: extras are only attempted through `require_extra` effects, and the existing skip-with-warning path remains when a required category has no eligible matches. Rewrite generator tests so any extra-category match can satisfy `require_extra` and no assertion still depends on base-linked extra compatibility fields.</action>
  <verify>
    <automated>npm test -- src/services/generator.test.ts</automated>
  </verify>
  <acceptance_criteria>Generator logic contains no extra/base compatibility helper or fallback fill, rule-driven extra selection keys only on explicit `require_extra` categories, and the generator tests cover the no-default-extra and skip-with-warning cases.</acceptance_criteria>
  <done>Auto-generation treats extras as explicit rule-driven add-ons only, with no hidden compatibility narrowing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the phase with unchanged curry regressions and a complete validation contract</name>
  <files>src/services/curry-compatibility-regression.test.ts, .planning/phases/1000-remove-the-compatability-base-for-extras/1000-VALIDATION.md</files>
  <read_first>AGENTS.md, .planning/phases/1000-remove-the-compatability-base-for-extras/1000-VALIDATION.md, .planning/phases/20-compatibility-regression-coverage/20-01-SUMMARY.md, src/services/curry-compatibility-regression.test.ts, src/services/generator.test.ts</read_first>
  <behavior>
    - Test 1: Existing curry compatibility backbone assertions still pass with extras no longer carrying compatibility data.
    - Test 2: The validation artifact maps every Phase 1000 task to a real focused command and a final phase gate.
  </behavior>
  <action>Add or tighten one focused regression proving the extras cleanup does not alter the curry compatibility contract per D-10, preferably by running through the existing backbone seam rather than creating a second milestone harness. Then update `1000-VALIDATION.md` to Nyquist-compliant status with the actual plan/task IDs, focused commands, wave checks, and final gate command for this phase.</action>
  <verify>
    <automated>npm test -- src/services/curry-compatibility-regression.test.ts && npm test -- src/components/library/ComponentForm.test.tsx src/components/plan/MealPickerSheet.test.tsx src/services/food-db.test.ts src/services/generator.test.ts src/db/migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>Curry compatibility regressions still pass after the extra cleanup, and `1000-VALIDATION.md` becomes a complete executable validation contract tied to the finished plans.</acceptance_criteria>
  <done>The phase has automated proof for explicit-only extras and unchanged curry compatibility, plus a ready-to-approve validation artifact.</done>
</task>

</tasks>

<verification>
Run the focused generator suite after Task 1, then run the backbone plus phase-focused command set after Task 2. Do not close the plan until `1000-VALIDATION.md` has real task IDs, command rows, and `nyquist_compliant: true`.
</verification>

<success_criteria>
Generated slots never receive extras unless a `require_extra` rule explicitly requires them, missing required extras still warn instead of fallback-filling, and the existing curry compatibility regression backbone stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-remove-the-compatability-base-for-extras/1000-03-SUMMARY.md`
</output>
