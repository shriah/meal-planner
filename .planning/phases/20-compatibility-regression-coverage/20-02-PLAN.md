---
phase: 20-compatibility-regression-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/plan/MealPickerSheet.test.tsx
  - src/stores/plan-store.test.ts
  - src/components/library/ComponentForm.test.tsx
  - src/components/library/ComponentRow.test.tsx
  - .planning/phases/20-compatibility-regression-coverage/20-VALIDATION.md
autonomous: true
requirements:
  - CURRY-08
must_haves:
  truths:
    - "Supporting regressions prove the library, picker, and locked/manual runtime seams all reflect the same compatibility contract as the backbone harness."
    - "Picker and locked/manual tests keep proving incompatible curries are exceptions, not default matches, after the normalization-focused backbone lands."
    - "Phase validation maps CURRY-08 to one clear backbone run, one supporting seam run, and the phase gate without requiring manual verification by default."
  artifacts:
    - path: "src/components/plan/MealPickerSheet.test.tsx"
      provides: "Focused picker-after-normalization and override-support regressions"
    - path: "src/stores/plan-store.test.ts"
      provides: "Focused locked/manual runtime regressions that support the CURRY-08 backbone"
    - path: "src/components/library/ComponentForm.test.tsx"
      provides: "Library regression coverage for rename-safe curry compatibility editing"
    - path: "src/components/library/ComponentRow.test.tsx"
      provides: "Library regression coverage for rename-safe compatibility summaries and zero-compatible state"
    - path: ".planning/phases/20-compatibility-regression-coverage/20-VALIDATION.md"
      provides: "Nyquist-ready validation contract mapping CURRY-08 to the backbone and supporting commands"
  key_links:
    - from: "src/components/plan/MealPickerSheet.test.tsx"
      to: "src/components/plan/MealPickerSheet.tsx"
      via: "compatible vs override section behavior after normalized category inputs"
      pattern: "Compatible Curries|Override Choices"
    - from: "src/stores/plan-store.test.ts"
      to: "src/stores/plan-store.ts"
      via: "swapComponent/regenerate preservation of explicit curry intent"
      pattern: "(swapComponent|regenerate)"
    - from: ".planning/phases/20-compatibility-regression-coverage/20-VALIDATION.md"
      to: "CURRY-08"
      via: "requirement-to-command traceability"
      pattern: "CURRY-08"
---

<objective>
Lock the downstream supporting regressions for `CURRY-08` and pre-create the Phase 20 validation contract so the milestone proof is executable, traceable, and automated by default.

Purpose: Support the backbone regression with the focused picker, store, and library proofs the milestone still needs, while keeping manual verification optional per D-09.
Output: Supporting downstream regressions plus a concrete `20-VALIDATION.md` that maps `CURRY-08` to the smallest useful command set.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-compatibility-regression-coverage/20-CONTEXT.md
@.planning/phases/20-compatibility-regression-coverage/20-RESEARCH.md
@.planning/phases/17-curry-compatibility-data/17-VERIFICATION.md
@.planning/phases/19-explicit-override-paths/19-VERIFICATION.md
@src/components/plan/MealPickerSheet.test.tsx
@src/stores/plan-store.test.ts
@src/components/library/ComponentForm.test.tsx
@src/components/library/ComponentRow.test.tsx

<interfaces>
From src/components/plan/MealPickerSheet.tsx:
```typescript
interface MealPickerSheetProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  day: DayOfWeek
  slot: MealSlot
  componentType: 'base' | 'curry' | 'subzi' | 'extras'
  currentBaseCategoryId?: number
}
```

From src/stores/plan-store.ts:
```typescript
swapComponent: (
  day: DayOfWeek,
  slot: MealSlot,
  componentType: 'base' | 'curry' | 'subzi' | 'extras',
  componentId: number,
) => void

regenerate: () => Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand focused picker, store, and library regressions so they support the backbone contract</name>
  <files>src/components/plan/MealPickerSheet.test.tsx, src/stores/plan-store.test.ts, src/components/library/ComponentForm.test.tsx, src/components/library/ComponentRow.test.tsx</files>
  <behavior>
    - Test 1: the library tests keep proving rename-safe curry compatibility labels and editable compatibility state after category normalization.
    - Test 2: the picker tests prove post-normalization runtime choices still classify curries as compatible vs explicit override choices under the shipped contract.
    - Test 3: the store tests keep proving manual and locked incompatible curry intent survives regenerate unchanged and supports, rather than replaces, the backbone contract.
  </behavior>
  <action>Add only the focused assertions needed to support D-02, D-04, D-05, and D-06 without duplicating the new backbone file. Expand the existing library tests where rename-safe compatibility editing or zero-compatible presentation still need to be reasserted as part of the milestone contract. Expand picker/store tests only where they add downstream runtime proof after normalization or explicit override preservation. Keep all changes test-only and avoid reshaping production components or store code unless a real drift is exposed, per D-07 and D-08.</action>
  <verify>
    <automated>npx vitest run src/components/plan/MealPickerSheet.test.tsx src/stores/plan-store.test.ts src/components/library/ComponentForm.test.tsx src/components/library/ComponentRow.test.tsx</automated>
  </verify>
  <done>The downstream seams still prove the same compatibility rules as the backbone: rename-safe library editing, explicit picker override classification, and locked/manual preservation of explicit curry intent.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-create the Phase 20 validation contract with clean CURRY-08 traceability</name>
  <files>.planning/phases/20-compatibility-regression-coverage/20-VALIDATION.md</files>
  <action>Create `20-VALIDATION.md` during plan execution so Nyquist gating has a concrete artifact before implementation starts. Keep it pending initially, map `CURRY-08` directly to the dedicated backbone regression plus the focused supporting seam run and final phase gate, and avoid bloated or redundant command lists. Manual verification should stay out unless execution reveals a real UI-only gap, per D-09. The validation language should make it explicit that Phase 20 adds proof only and does not add new compatibility behavior, per D-07.</action>
  <verify>
    <automated>rg -n "status: pending|CURRY-08|curry-compatibility-regression.test.ts|MealPickerSheet.test.tsx|plan-store.test.ts|ComponentForm.test.tsx|ComponentRow.test.tsx|npm test" .planning/phases/20-compatibility-regression-coverage/20-VALIDATION.md</automated>
  </verify>
  <done>The phase has a concrete validation artifact that traces CURRY-08 to the backbone test, the focused supporting regressions, and the final phase gate without requiring manual checks by default.</done>
</task>

</tasks>

<verification>
Focused downstream and validation commands pass:
- `npx vitest run src/components/plan/MealPickerSheet.test.tsx src/stores/plan-store.test.ts src/components/library/ComponentForm.test.tsx src/components/library/ComponentRow.test.tsx`
- `rg -n "status: pending|CURRY-08|curry-compatibility-regression.test.ts|npm test" .planning/phases/20-compatibility-regression-coverage/20-VALIDATION.md`
</verification>

<success_criteria>
`CURRY-08` remains traceable beyond the backbone harness: the library, picker, and locked/manual seams still prove the same contract, and the phase has a Nyquist-ready automated validation file.
</success_criteria>

<output>
After completion, create `.planning/phases/20-compatibility-regression-coverage/20-02-SUMMARY.md`
</output>
