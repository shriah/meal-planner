---
phase: 14-add-option-to-create-more-base-category-and-extra-category
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/category.ts
  - src/types/component.ts
  - src/types/index.ts
  - src/lib/category-labels.ts
  - src/db/client.ts
  - src/services/category-db.ts
  - src/services/food-db.ts
  - src/db/migrations.test.ts
  - src/services/food-db.test.ts
autonomous: true
requirements: [CAT-01, CAT-02, CAT-06, CAT-07]
must_haves:
  truths:
    - "Built-in base and extra categories exist as persisted records with stable IDs instead of hard-coded literals"
    - "Components and rules can keep referring to category IDs after a rename because labels resolve from live category records"
    - "Deleting a category clears or strips dependent references so no dangling category IDs remain"
  artifacts:
    - path: "src/types/category.ts"
      provides: "Category record and category-kind contracts shared across UI, services, and rules"
    - path: "src/db/client.ts"
      provides: "Dexie schema with categories table and Phase 14 migration from literal category names to IDs"
    - path: "src/services/category-db.ts"
      provides: "Category CRUD and transactional delete normalization helpers"
    - path: "src/services/food-db.ts"
      provides: "Component queries updated for category-ID storage"
    - path: "src/db/migrations.test.ts"
      provides: "Migration coverage for literal-to-ID rewrite and delete normalization invariants"
    - path: "src/services/food-db.test.ts"
      provides: "Service coverage for category CRUD, compatibility lookups, and normalized deletes"
  key_links:
    - from: "src/db/client.ts"
      to: "src/services/category-db.ts"
      via: "Shared category table and transactional cleanup rules"
      pattern: "categories|deleteCategory|normalize"
    - from: "src/services/category-db.ts"
      to: "src/services/food-db.ts"
      via: "Category-ID based component lookups for extras and forms"
      pattern: "base_category_id|extra_category_id|compatible_base_category_ids"
    - from: "src/lib/category-labels.ts"
      to: "src/components/library/ComponentForm.tsx"
      via: "Stable-ID label resolution after rename and delete"
      pattern: "getCategoryLabel|Deleted category"
---

<objective>
Create the Phase 14 foundation: persisted category records, category-ID based component storage, and one shared delete-normalization path that later UI and rule plans can build on without leaving Wave 1 in a broken mixed-contract state.

Purpose: Per D-06, D-13, D-14, and D-15, Phase 14 only works if category identity is data-backed and delete fallout is normalized centrally instead of ad hoc in each surface.
Output: Category type/contracts, Dexie schema migration, category CRUD service, backward-compatible component data access, and regression coverage for migration plus delete normalization.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-CONTEXT.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-RESEARCH.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VALIDATION.md
@src/types/component.ts
@src/db/client.ts
@src/services/food-db.ts
@src/db/migrations.test.ts
@src/services/food-db.test.ts

<interfaces>
From `src/types/component.ts`:
```ts
export interface BaseRecord extends BaseComponentFields {
  componentType: 'base';
  base_type: BaseType;
}

export interface ExtraRecord extends BaseComponentFields {
  componentType: 'extra';
  extra_category: ExtraCategory;
  compatible_base_types: BaseType[];
}
```

From `src/services/food-db.ts`:
```ts
export async function getComponentsByType(type: ComponentType): Promise<ComponentRecord[]>;
export async function getExtrasByBaseType(baseType: BaseType): Promise<ComponentRecord[]>;
export async function addComponent(component: Omit<ComponentRecord, 'id'>): Promise<number>;
export async function updateComponent(id: number, changes: Partial<ComponentRecord>): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define category contracts and red-line migration expectations</name>
  <files>src/types/category.ts, src/types/component.ts, src/types/index.ts, src/lib/category-labels.ts, src/db/migrations.test.ts, src/services/food-db.test.ts</files>
  <read_first>
    - src/types/component.ts
    - src/types/index.ts
    - src/db/client.ts
    - src/services/food-db.ts
    - src/db/migrations.test.ts
    - src/services/food-db.test.ts
    - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-CONTEXT.md
    - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: per CAT-01 and D-07/D-08, Phase 14 migration creates `base` and `extra` category records for the existing built-in labels.
    - Test 2: per CAT-02, base and extra component records persist numeric category IDs, and extra compatibility persists `compatible_base_category_ids`.
    - Test 3: per CAT-07 and D-14/D-15, deleting a category strips deleted IDs from multi-value fields, clears direct component references, and disables rules whose target category becomes invalid.
  </behavior>
  <action>
    Create `src/types/category.ts` with exported `CategoryKind = 'base' | 'extra'` and `CategoryRecord` containing `id?: number`, `kind`, `name`, and `created_at`. Update `src/types/component.ts` so the canonical persisted category fields become ID-based: `base_category_id?: number | null`, `extra_category_id?: number | null`, and `compatible_base_category_ids?: number[]`. Keep tag unions unchanged. Export the new category types from `src/types/index.ts`.

    Keep Plan 14-01 backward-compatible for one wave. The migration may write canonical IDs immediately, but untouched callers in Plans 14-02 and 14-03 must not be stranded on a dead contract after Wave 1. Preserve temporary compatibility through type aliases, normalization helpers, or service-layer shims for `base_type`, `extra_category`, and `compatible_base_types` until those downstream plans land and remove the last consumers.

    Add `src/lib/category-labels.ts` with pure helpers that build a `Map<number, CategoryRecord>` and resolve labels via explicit fallbacks like `Deleted base category` and `Deleted extra category`; later plans must reuse these helpers for D-04 and D-13 instead of rewriting names into references.

    Rewrite `src/db/migrations.test.ts` and `src/services/food-db.test.ts` first so they fail against the pre-Phase-14 model. Include fixtures that prove literal category names migrate to IDs, built-ins become rows in one `categories` table, delete normalization clears/strips category references, and renamed category labels resolve from category rows without stored-ID churn.
  </action>
  <acceptance_criteria>
    - `rg -n "CategoryKind|CategoryRecord" src/types/category.ts` returns both exports.
    - `rg -n "base_category_id|extra_category_id|compatible_base_category_ids" src/types/component.ts` returns all 3 canonical ID fields.
    - `rg -n "base_type|extra_category|compatible_base_types|getExtrasByBaseType" src/types/component.ts src/services/food-db.ts` returns either transitional compatibility shims or explicit removal only after downstream callers no longer depend on them.
    - `rg -n "categories table|delete normalization|rename resolves via labels" src/db/migrations.test.ts src/services/food-db.test.ts` returns the new Phase 14 coverage names or comments.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/db/migrations.test.ts src/services/food-db.test.ts</automated>
  </verify>
  <done>Foundation tests describe the new category-ID model, shared label resolution helpers exist, and Wave 1 leaves the repo in a transitional but still runnable state for the downstream UI and rules cutovers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement categories table, migration chain, and shared delete-normalization services</name>
  <files>src/db/client.ts, src/services/category-db.ts, src/services/food-db.ts</files>
  <read_first>
    - src/db/client.ts
    - src/services/food-db.ts
    - src/types/category.ts
    - src/types/component.ts
    - src/lib/category-labels.ts
    - src/db/migrations.test.ts
    - src/services/food-db.test.ts
    - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-RESEARCH.md
  </read_first>
  <action>
    In `src/db/client.ts`, add a typed `categories` table to the Dexie singleton and introduce a new database version that:
    1. seeds built-in base categories `rice-based`, `bread-based`, `other` and extra categories `liquid`, `crunchy`, `condiment`, `dairy`, `sweet` as normal rows per D-07/D-08,
    2. rewrites legacy component and compiled-rule category literals to numeric category IDs,
    3. preserves IDs through rename by leaving references untouched,
    4. leaves no dangling category names after migration.

    Create `src/services/category-db.ts` as the single category-management service from the research recommendation. It must export read helpers for categories by kind, add/rename/delete operations, and one transactional `deleteCategory(id)` path that removes the category row, clears direct component category references, strips deleted IDs from compatibility arrays and `require_extra` effects, and disables rules whose category-backed target becomes invalid, per D-05, D-14, and CAT-07.

    Update `src/services/food-db.ts` so category-aware queries use IDs, especially the extra-compatibility query used by forms and the meal picker. Keep the service layer pure TypeScript with no React and no component-side direct Dexie writes. Maintain transitional helpers or adapters where needed so Wave 1 still boots and passes verification before the library/rules plans cut over.
  </action>
  <acceptance_criteria>
    - `rg -n "categories: '.*kind" src/db/client.ts` shows the categories table declared in Dexie stores.
    - `rg -n "export async function (getCategoriesByKind|addCategory|renameCategory|deleteCategory)" src/services/category-db.ts` returns all four service entry points.
    - `rg -n "compatible_base_category_ids|base_category_id|extra_category_id" src/services/food-db.ts src/db/client.ts` returns category-ID based access paths.
    - `rg -n "disable.*rule|enabled = false|effects = .*filter" src/services/category-db.ts src/db/client.ts` returns delete-normalization logic for invalidated rules.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/db/migrations.test.ts src/services/food-db.test.ts</automated>
  </verify>
  <done>The app has one persisted categories source of truth, component data access uses category IDs, and delete normalization is centralized in a reusable service rather than scattered across UI handlers.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/db/migrations.test.ts src/services/food-db.test.ts`. This plan is complete only when built-in categories exist as records, component/category references are ID-based, and category deletion leaves no dangling IDs in components or rules.
</verification>

<success_criteria>
CAT-01, CAT-02, CAT-06, and CAT-07 have a working foundation: categories are persisted data, rename is label-driven, delete cleanup is transactional, and downstream plans can consume category IDs without Wave 1 breaking still-unmigrated callers.
</success_criteria>

<output>
After completion, create `.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-01-SUMMARY.md`
</output>
