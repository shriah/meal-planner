---
phase: 20-compatibility-regression-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations.test.ts
  - src/services/curry-compatibility-regression.test.ts
  - src/services/food-db.test.ts
  - src/services/generator.test.ts
autonomous: true
requirements:
  - CURRY-08
must_haves:
  truths:
    - "The milestone has one readable backbone regression proving actual migration/backfill proof, default generation, and explicit override behavior all follow the same curry compatibility contract."
    - "Rename/delete normalization is proved both in stored compatibility data and in downstream runtime generation behavior after normalization."
    - "The regression proof shows deleted or renamed category references no longer influence automatic curry selection after normalization."
  artifacts:
    - path: "src/db/migrations.test.ts"
      provides: "Executable upgrade-path proof that legacy curries become compatibility-aware records with the Phase 17 backfill contract intact"
    - path: "src/services/curry-compatibility-regression.test.ts"
      provides: "Dedicated CURRY-08 backbone regression harness"
    - path: "src/services/food-db.test.ts"
      provides: "Focused service-level rename/delete normalization proof tied to runtime-safe compatibility inputs"
    - path: "src/services/generator.test.ts"
      provides: "Focused runtime proof that normalized compatibility state drives default and explicit curry selection correctly"
  key_links:
    - from: "src/services/curry-compatibility-regression.test.ts"
      to: "src/services/generator.ts"
      via: "Dexie-backed generation flow seeded from compatibility-aware data"
      pattern: "generate"
    - from: "src/services/food-db.test.ts"
      to: "src/services/category-db.ts"
      via: "renameCategory/deleteCategory normalization"
      pattern: "renameCategory|deleteCategory"
    - from: "src/services/generator.test.ts"
      to: "src/services/generator.ts"
      via: "post-normalization runtime assertions for default and explicit curry paths"
      pattern: "compatibleCurries|lockedSlots|require_one"
---

<objective>
Create the Phase 20 backbone proof for `CURRY-08` and extend the existing service/runtime tests so rename/delete normalization is shown to hold all the way through downstream generator behavior.

Purpose: Lock the shipped curry compatibility contract at the milestone level without adding new behavior, per D-01 through D-08.
Output: Actual migration/backfill proof plus one dedicated milestone regression file and focused service/generator regressions that prove normalization-safe runtime behavior.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-compatibility-regression-coverage/20-CONTEXT.md
@.planning/phases/20-compatibility-regression-coverage/20-RESEARCH.md
@.planning/phases/17-curry-compatibility-data/17-VERIFICATION.md
@.planning/phases/18-generator-compatibility-contract/18-VERIFICATION.md
@.planning/phases/19-explicit-override-paths/19-VERIFICATION.md
@src/services/generator.ts
@src/services/food-db.test.ts
@src/services/generator.test.ts
@src/services/category-db.ts
@src/db/migrations.test.ts

<interfaces>
From src/services/generator.ts:
```typescript
export interface GenerateOptions {
  lockedSlots?: Partial<Record<`${DayOfWeek}-${MealSlot}`, {
    base_id?: number
    curry_id?: number
    subzi_id?: number
    extra_ids?: number[]
  }>>
}

export async function generate(options?: GenerateOptions): Promise<GeneratorResult>
```

From src/services/category-db.ts:
```typescript
export async function renameCategory(id: number, name: string): Promise<void>
export async function deleteCategory(id: number): Promise<void>
```

From src/services/food-db.ts:
```typescript
export async function getAllComponents(): Promise<ComponentRecord[]>
export async function addComponent(component: Omit<ComponentRecord, 'id'>): Promise<number>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add actual migration/backfill proof and the dedicated CURRY-08 backbone regression harness</name>
  <files>src/db/migrations.test.ts, src/services/curry-compatibility-regression.test.ts</files>
  <behavior>
    - Test 0: actual migration/backfill tests prove legacy curries become compatibility-aware records with curated seeded mappings, unmatched all-base fallback, and explicit empty-array preservation still intact.
    - Test 1: one milestone-level regression story seeds compatibility-aware data and proves default generation, explicit override behavior, and locked/manual preservation all align on the same curry/base contract.
    - Test 2: the backbone proves the migrated/backfilled contract produces usable runtime behavior without reintroducing permissive incompatible defaults.
    - Test 3: the backbone remains test-only and does not require new production helpers or behavior changes.
  </behavior>
  <action>Extend `src/db/migrations.test.ts` first so Phase 20 has executable upgrade-path proof for success criterion 1, not just runtime fixtures. Keep the assertions tied to the actual Phase 17 backfill contract: curated seeded mappings, unmatched legacy all-base fallback, and preservation of intentional empty arrays. Then create `src/services/curry-compatibility-regression.test.ts` as the primary evidence artifact for D-01, D-02, and D-03. Keep it service-first and Dexie-backed rather than UI-heavy: seed a minimal fixture that reads like the milestone story, then assert the same contract across default generation, explicit `require_one` override handling, and locked/manual preservation. Reuse existing generator/store seams instead of inventing new production helpers per D-07 and D-08. The two files together should give a verifier clear proof for both the upgrade path and the milestone-wide runtime contract.</action>
  <verify>
    <automated>npx vitest run src/db/migrations.test.ts src/services/curry-compatibility-regression.test.ts</automated>
  </verify>
  <done>The repo has executable upgrade-path proof plus one dedicated milestone regression file that clearly proves the shipped compatibility contract across the migrated/default/override paths without changing production behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove rename/delete normalization through service data and downstream generator runtime</name>
  <files>src/services/food-db.test.ts, src/services/generator.test.ts</files>
  <behavior>
    - Test 1: rename/delete normalization removes stale category references from persisted curry compatibility data without collapsing intentional empty arrays or inventing fallback compatibility.
    - Test 2: after normalization, automatic generation no longer behaves as if deleted or renamed compatibility categories still exist.
    - Test 3: explicit override paths remain narrow and intentional after normalization, rather than broadening default runtime behavior.
  </behavior>
  <action>Extend the focused service and generator test seams per D-04, D-05, and D-06 so normalization proof crosses the data/runtime boundary. In `src/services/food-db.test.ts`, strengthen rename/delete assertions around the normalized compatibility arrays and runtime-safe inputs. In `src/services/generator.test.ts`, add post-normalization runtime regressions showing deleted or renamed category references no longer influence automatic curry selection while explicit override flows still behave exactly as already shipped. Do not add new fallback rules, override controls, or production behavior in this phase per D-07.</action>
  <verify>
    <automated>npx vitest run src/services/food-db.test.ts src/services/generator.test.ts</automated>
  </verify>
  <done>Rename/delete normalization is proved both at the service layer and in real runtime generation behavior, and the tests show no stale compatibility behavior survives after normalization.</done>
</task>

</tasks>

<verification>
Focused backbone and runtime normalization commands pass:
- `npx vitest run src/db/migrations.test.ts src/services/curry-compatibility-regression.test.ts`
- `npx vitest run src/services/food-db.test.ts src/services/generator.test.ts`
</verification>

<success_criteria>
`CURRY-08` has a primary executable backbone proof, and rename/delete normalization is shown to remain correct from stored compatibility data through downstream generator behavior after normalization.
</success_criteria>

<output>
After completion, create `.planning/phases/20-compatibility-regression-coverage/20-01-SUMMARY.md`
</output>
