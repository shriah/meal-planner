---
phase: 19-explicit-override-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/plan/MealPickerSheet.tsx
  - src/components/plan/MealPickerSheet.test.tsx
  - src/stores/plan-store.ts
  - src/stores/plan-store.test.ts
autonomous: true
requirements:
  - CURRY-05
must_haves:
  truths:
    - "User sees compatible curries as the default choices and incompatible curries as explicit override choices when both sets exist."
    - "User can still pick an incompatible curry from the manual picker when they intentionally want that pairing."
    - "A manually chosen incompatible curry remains the stored slot value and survives explicit lock/regenerate flows until the user replaces it."
  artifacts:
    - path: "src/components/plan/MealPickerSheet.tsx"
      provides: "Grouped curry picker presentation with compatible/incompatible sections"
    - path: "src/components/plan/MealPickerSheet.test.tsx"
      provides: "Picker regressions for grouped and flat-list override behavior"
    - path: "src/stores/plan-store.ts"
      provides: "Manual swap and regenerate handling that preserves explicit curry intent"
    - path: "src/stores/plan-store.test.ts"
      provides: "Store regressions for manual incompatible persistence and regenerate wiring"
  key_links:
    - from: "src/components/plan/MealPickerSheet.tsx"
      to: "src/stores/plan-store.ts"
      via: "swapComponent(day, slot, 'curry', componentId)"
      pattern: "swapComponent\\("
    - from: "src/stores/plan-store.ts"
      to: "src/services/generator.ts"
      via: "generate({ lockedSlots }) during regenerate"
      pattern: "generate\\(\\{ lockedSlots \\}\\)"
---

<objective>
Make manual incompatible curry selection feel intentional in the picker and preserve that explicit user choice through the existing store/regeneration path.

Purpose: Deliver CURRY-05 without weakening the compatible-by-default generator contract from Phase 18.
Output: Grouped curry picker behavior plus store regressions that lock manual incompatible intent into the saved plan and lock/regenerate flow.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-explicit-override-paths/19-CONTEXT.md
@.planning/phases/19-explicit-override-paths/19-RESEARCH.md
@.planning/phases/18-generator-compatibility-contract/18-01-SUMMARY.md
@src/components/plan/MealPickerSheet.tsx
@src/stores/plan-store.ts
@src/stores/plan-store.test.ts

<interfaces>
From src/components/plan/MealPickerSheet.tsx:
```typescript
interface MealPickerSheetProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  day: DayOfWeek
  slot: MealSlot
  componentType: 'base' | 'curry' | 'subzi' | 'extras'
  currentBaseCategoryId?: number
}
```

From src/stores/plan-store.ts:
```typescript
swapComponent: (
  day: DayOfWeek,
  slot: MealSlot,
  componentType: 'base' | 'curry' | 'subzi' | 'extras',
  componentId: number,
) => void

regenerate: () => Promise<void>
```

From src/services/generator.ts:
```typescript
export interface GenerateOptions {
  lockedSlots?: Partial<Record<`${DayOfWeek}-${MealSlot}`, {
    base_id?: number
    curry_id?: number
    subzi_id?: number
    extra_ids?: number[]
  }>>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Group curry picker results into normal and override sections</name>
  <files>src/components/plan/MealPickerSheet.tsx, src/components/plan/MealPickerSheet.test.tsx</files>
  <behavior>
    - Test 1: when a base has both compatible and incompatible curries, the picker renders separate sections and keeps incompatible entries selectable as exceptions.
    - Test 2: when a base has zero compatible curries, the picker falls back to one flat list instead of rendering an empty "compatible" section.
    - Test 3: base, subzi, and extras picker flows keep their current flat-list behavior.
  </behavior>
  <action>Update the curry branch of `MealPickerSheet` to derive compatible vs incompatible groupings using the same compatibility contract Phase 18 established, including the legacy uncategorized-base edge case. Implement grouped rendering per D-01 and D-03, but collapse back to one list per D-02 when no compatible section exists. Keep selection behavior unchanged so incompatible curries remain explicit manual overrides, not disabled items. Do not add board badges, warning banners, or any new override control per D-08, D-10, and D-11.</action>
  <verify>
    <automated>npx vitest run src/components/plan/MealPickerSheet.test.tsx</automated>
  </verify>
  <done>The curry picker shows meaningful compatible/incompatible grouping only when both groups exist, incompatible choices are still selectable, and non-curry picker modes behave exactly as before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve manual incompatible curry intent through store persistence and regenerate wiring</name>
  <files>src/stores/plan-store.ts, src/stores/plan-store.test.ts</files>
  <behavior>
    - Test 1: a manual curry swap writes the exact incompatible `curry_id` into the stored plan and persists it to Dexie without any compatibility normalization.
    - Test 2: regeneration forwards locked/manual incompatible curry choices back into `generate({ lockedSlots })` unchanged.
    - Test 3: no new override metadata, warning UI, or silent "fix invalid curry" branch is introduced.
  </behavior>
  <action>Add focused store regressions that codify D-04, D-05, D-06, and D-12 around explicit incompatible user intent. If the current store flow already preserves the required behavior, keep implementation changes minimal and let the tests lock it in. If a narrow store adjustment is required, make it inside the existing `swapComponent`/`regenerate` seam without adding a new persisted override flag or auto-normalizing incompatible curries back to compatible ones.</action>
  <verify>
    <automated>npx vitest run src/stores/plan-store.test.ts</automated>
  </verify>
  <done>Manual incompatible curry selections remain exact in store persistence, and explicit lock/regenerate flows preserve that choice without the app trying to "repair" it.</done>
</task>

</tasks>

<verification>
Focused UI and store regressions pass:
- `npx vitest run src/components/plan/MealPickerSheet.test.tsx`
- `npx vitest run src/stores/plan-store.test.ts`
</verification>

<success_criteria>
CURRY-05 is satisfied: the picker exposes incompatible curries as explicit exceptions, and manual incompatible choices persist through the existing saved-plan plus lock/regenerate path without becoming normal auto-generated output.
</success_criteria>

<output>
After completion, create `.planning/phases/19-explicit-override-paths/19-01-SUMMARY.md`
</output>
