---
phase: 17-curry-compatibility-data
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/db/client.ts
  - src/db/migrations.test.ts
  - src/services/food-db.test.ts
autonomous: true
requirements: [CURRY-02, CURRY-07]
must_haves:
  truths:
    - "Upgrading an existing library backfills legacy curries into editable `compatible_base_category_ids` without forcing recreation."
    - "Curated seeded curries upgrade to curated compatibility arrays, while unmatched legacy curries safely fall back to all current base category IDs."
    - "Deleting a base category strips that ID from curry compatibility arrays and leaves zero-compatible curries empty instead of resetting them."
  artifacts:
    - path: "src/db/client.ts"
      provides: "Dexie migration/backfill and curry-aware delete normalization"
    - path: "src/db/migrations.test.ts"
      provides: "Upgrade-path coverage for curated mapping, all-base fallback, and explicit empty-array preservation"
    - path: "src/services/food-db.test.ts"
      provides: "Service-level regression coverage for curry delete normalization"
  key_links:
    - from: "src/db/client.ts"
      to: "src/db/seed-data.ts"
      via: "Shared curated curry compatibility resolver used during upgrade"
      pattern: "compatible_base_category_ids|curry"
    - from: "src/services/category-db.ts"
      to: "src/db/client.ts"
      via: "deleteCategory -> normalizeComponentCategoryRefs"
      pattern: "normalizeComponentCategoryRefs"
    - from: "src/db/migrations.test.ts"
      to: "src/db/client.ts"
      via: "Migration fixture assertions for legacy curry backfill"
      pattern: "compatible_base_category_ids|normalizeDeletedCategory"
---

<objective>
Upgrade persisted curry rows into the new compatibility model and extend delete normalization so category lifecycle changes remain ID-safe after v1.3.

Purpose: Phase 17 must preserve existing user libraries. This plan delivers the one-time Dexie upgrade, preserves the explicit-empty-array contract, and guarantees deleted base categories are normalized out of curry compatibility without silently broadening the constraint again.
Output: Curry-aware migration/backfill logic and automated upgrade/delete regressions.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-curry-compatibility-data/17-CONTEXT.md
@.planning/phases/17-curry-compatibility-data/17-RESEARCH.md
@.planning/phases/17-curry-compatibility-data/17-01-SUMMARY.md
@src/db/client.ts
@src/db/migrations.test.ts
@src/services/category-db.ts
@src/services/food-db.test.ts
@src/db/seed-data.ts
@src/types/component.ts

<interfaces>
From `src/db/client.ts`:
```typescript
function normalizeComponentCategoryRefs(
  component: ComponentRecord,
  category: CategoryRecord & { id: number },
): ComponentRecord { ... }

db.version(11).stores({
  categories: '++id, kind, name',
  components: '++id, componentType, base_type, base_category_id, extra_category, extra_category_id, *compatible_base_types, *compatible_base_category_ids, *dietary_tags, *regional_tags, *occasion_tags',
}).upgrade(async (tx) => { ... });
```

From `src/services/category-db.ts`:
```typescript
export async function deleteCategory(id: number): Promise<void> {
  await db.transaction('rw', db.categories, db.components, db.rules, async () => {
    const nextComponent = normalizeComponentCategoryRefs(component, category);
    await db.components.update(component.id!, nextComponent);
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Dexie curry backfill migration for curated and unmatched legacy rows</name>
  <files>src/db/client.ts, src/db/migrations.test.ts</files>
  <read_first>
    - src/db/client.ts
    - src/db/migrations.test.ts
    - src/db/seed-data.ts
    - src/types/component.ts
    - .planning/phases/17-curry-compatibility-data/17-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: per D-01 and CURRY-02, a legacy seeded/default curry upgrades to its curated `compatible_base_category_ids`.
    - Test 2: per D-02 and CURRY-02, an unmatched legacy curry with `compatible_base_category_ids === undefined` upgrades to all current base-category IDs.
    - Test 3: per D-04, an existing explicit `compatible_base_category_ids: []` survives migration unchanged and is not expanded back to all bases.
  </behavior>
  <action>
    Add a new Dexie version upgrade in `src/db/client.ts` that rewrites existing curry rows exactly once. Use the shared curated resolver from Plan 17-01 for seeded/default curries per D-01, and fall back unmatched legacy curries to all currently known base-category IDs per D-02 so existing libraries remain usable after upgrade. The migration must treat only `undefined` as "needs backfill"; preserve any actual array value, including `[]`, per D-04 and the explicit-empty-array contract.

    Update `src/db/migrations.test.ts` first so the upgrade path proves the curated-map path, the all-bases fallback path, and the explicit-empty preservation path. Keep matching exact and deterministic; do not infer curated mappings from partial names, and do not implement any generator behavior in this migration.
  </action>
  <verify>
    <automated>npx vitest run src/db/migrations.test.ts</automated>
  </verify>
  <done>Opening the v1.3 database upgrades legacy curry rows into explicit category-ID compatibility arrays without collapsing explicit `[]` into a fallback value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize deleted base-category IDs out of curry compatibility and keep zero-compatible curries empty</name>
  <files>src/db/client.ts, src/services/food-db.test.ts</files>
  <behavior>
    - Test 1: per D-10 and CURRY-07, deleting a base category removes that ID from a curry's `compatible_base_category_ids`.
    - Test 2: per D-11, if that removal leaves the curry with zero compatible IDs, the stored value remains `[]` instead of resetting to all bases.
    - Test 3: per CURRY-07, rename/delete safety remains category-ID-based and does not reintroduce label-backed normalization.
  </behavior>
  <action>
    Extend the existing category normalization seam in `src/db/client.ts` so curry compatibility is treated as a first-class component reference during base-category deletes, not just an extras-adjacent field that happens to share a property name. Preserve the current ID-based contract from Phase 14: renames keep IDs stable, deletes strip the removed ID, and zero-compatible curries stay explicitly empty per D-10 and D-11.

    Add focused service-level regression coverage in `src/services/food-db.test.ts` that exercises `deleteCategory()` against curry rows, proving the normalized result and guarding against a regression that would silently re-expand empty arrays or leave deleted IDs behind.
  </action>
  <verify>
    <automated>npx vitest run src/services/food-db.test.ts src/db/migrations.test.ts</automated>
  </verify>
  <done>Category delete normalization removes stale base-category IDs from curries and preserves explicit zero-compatible states, keeping compatibility rename/delete-safe.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/db/migrations.test.ts src/services/food-db.test.ts`. Confirm legacy curries upgrade with curated or all-base compatibility as appropriate, explicit empty arrays remain untouched, and deleting a base category strips stale IDs from curry compatibility without broadening the stored constraint.
</verification>

<success_criteria>
Existing libraries survive the v1.3 upgrade: legacy curries become editable compatibility-aware records, category delete normalization remains ID-safe, and no migration path requires rebuilding the library.
</success_criteria>

<output>
After completion, create `.planning/phases/17-curry-compatibility-data/17-02-SUMMARY.md`
</output>
