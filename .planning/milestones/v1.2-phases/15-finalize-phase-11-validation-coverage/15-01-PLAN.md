---
phase: 15-finalize-phase-11-validation-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-edit-rule/11-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [audit-tech-debt-phase-11-validation]
must_haves:
  truths:
    - "Phase 11's validation artifact reflects the completed edit-rule work instead of draft pre-execution placeholders."
    - "Every Phase 11 task in the validation map points at an existing automated command and marks coverage as green once rerun."
    - "Phase 11 is Nyquist-compliant at the same level as Phases 12-14, with approved frontmatter and no unresolved Wave 0 gaps."
  artifacts:
    - path: ".planning/phases/11-edit-rule/11-VALIDATION.md"
      provides: "Approved Nyquist validation contract aligned to the shipped Phase 11 implementation and test surface"
  key_links:
    - from: ".planning/phases/11-edit-rule/11-VALIDATION.md"
      to: "src/components/rules/form-state.test.ts"
      via: "Per-task command map for 11-01 reducer coverage"
      pattern: "form-state\\.test\\.ts"
    - from: ".planning/phases/11-edit-rule/11-VALIDATION.md"
      to: "src/services/rule-compiler.test.ts"
      via: "Round-trip compiler/decompiler validation coverage"
      pattern: "rule-compiler\\.test\\.ts"
    - from: ".planning/phases/11-edit-rule/11-VALIDATION.md"
      to: "src/services/food-db.test.ts|src/components/rules/RuleRow.test.tsx"
      via: "Overwrite/edit interaction coverage for the shipped row edit flow"
      pattern: "food-db\\.test\\.ts|RuleRow\\.test\\.tsx"
---

<objective>
Close the remaining audit debt for Phase 11 by replacing the stale draft validation artifact with an approved Nyquist-compliant record of the tests and checks that already prove the shipped edit-rule behavior.

Purpose: The milestone audit identified a process gap, not a product gap. Phase 15 should finish the documentation and evidence trail so Phase 11's validation contract matches the standard established by Phases 12-14.
Output: An approved `.planning/phases/11-edit-rule/11-VALIDATION.md` with completed Wave 0 coverage, real command mapping, and sign-off grounded in rerun automated checks.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/11-edit-rule/11-VALIDATION.md
@.planning/phases/11-edit-rule/11-VERIFICATION.md
@.planning/phases/11-edit-rule/11-01-SUMMARY.md
@.planning/phases/11-edit-rule/11-02-SUMMARY.md
@.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md
@.planning/phases/13-only-include-extras-when-explicitly-required/13-VALIDATION.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase 11 validation to match the completed implementation and evidence</name>
  <files>.planning/phases/11-edit-rule/11-VALIDATION.md</files>
  <action>
    Update `.planning/phases/11-edit-rule/11-VALIDATION.md` so it no longer reads like a pre-execution draft. Use the passed evidence in `11-VERIFICATION.md`, `11-01-SUMMARY.md`, and `11-02-SUMMARY.md` to replace stale placeholders with the real finished test surface.

    Keep this phase tightly scoped to the audit debt only:
    - do not change product behavior, tests, or Phase 11 implementation files
    - do not introduce new requirements beyond the audit debt called out in `v1.2-MILESTONE-AUDIT.md`
    - do update frontmatter to approved/Nyquist-compliant states
    - do replace incorrect missing-file references with the actual existing test files and completed statuses
    - do rewrite the Wave 0 section from open gaps into completed coverage statements that mirror the Phase 12-14 validation style
    - do align the per-task verification map to the original two-plan split and the actual commands that prove `EDIT-01` through `EDIT-04`

    The finished document should clearly show that the phase debt was documentation hygiene: the edit flow was already shipped and verified, and Phase 15 simply brings its validation contract up to later-phase standards.
  </action>
  <verify>
    <automated>rg -n "status: approved|nyquist_compliant: true|wave_0_complete: true|11-01-01|11-01-02|11-02-01|11-02-02|Approval: approved" .planning/phases/11-edit-rule/11-VALIDATION.md</automated>
  </verify>
  <done>`11-VALIDATION.md` is rewritten as an approved artifact with no stale draft-only placeholders, no false missing-file claims, and a completed task-to-command coverage map for all Phase 11 work.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the focused Phase 11 checks and record final validation sign-off</name>
  <files>.planning/phases/11-edit-rule/11-VALIDATION.md</files>
  <action>
    Re-run the concrete automated checks that back the finished Phase 11 validation contract, then update the validation artifact to record successful sign-off. Use the focused command set already referenced across the Phase 11 verification artifacts:

    - `npx vitest run src/components/rules/form-state.test.ts src/services/rule-compiler.test.ts`
    - `npx vitest run src/services/food-db.test.ts src/components/rules/RuleRow.test.tsx`
    - `npx vitest run src/components/rules/form-state.test.ts src/services/rule-compiler.test.ts src/services/food-db.test.ts src/components/rules/RuleRow.test.tsx`
    - `npm test`

    Reflect those results back into `.planning/phases/11-edit-rule/11-VALIDATION.md`:
    - mark each task row green
    - keep the sampling-rate guidance concrete and consistent with the rerun commands
    - keep manual verification minimal and only where it adds value without reopening product scope
    - record approval text/date in the same style used by the approved Phase 12-14 validation files

    If any command fails, stop and capture the failure in the validation artifact instead of papering over it. The phase only closes if the artifact and the rerun evidence agree.
  </action>
  <verify>
    <automated>npx vitest run src/components/rules/form-state.test.ts src/services/rule-compiler.test.ts src/services/food-db.test.ts src/components/rules/RuleRow.test.tsx</automated>
  </verify>
  <done>Phase 11's focused regression suite has been rerun, the validation artifact records passing evidence and approval, and the milestone audit debt for partial Nyquist coverage is closed without any broad product changes.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/components/rules/form-state.test.ts src/services/rule-compiler.test.ts src/services/food-db.test.ts src/components/rules/RuleRow.test.tsx`, then `npm test`. Confirm `.planning/phases/11-edit-rule/11-VALIDATION.md` shows `status: approved`, `nyquist_compliant: true`, and `wave_0_complete: true`, with all four Phase 11 task rows marked green.
</verification>

<success_criteria>
The v1.2 milestone audit no longer has an open Phase 11 validation-hygiene gap: the shipped edit-rule work retains its existing behavior, Phase 11's validation artifact is approved and Nyquist-compliant, and later reviewers can trace every Phase 11 requirement to a real passing command in `11-VALIDATION.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/15-finalize-phase-11-validation-coverage/15-01-SUMMARY.md`
</output>
