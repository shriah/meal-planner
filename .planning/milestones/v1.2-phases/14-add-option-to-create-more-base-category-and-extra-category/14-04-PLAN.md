---
phase: 14-add-option-to-create-more-base-category-and-extra-category
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-03"]
files_modified:
  - src/services/generator.ts
  - src/services/generator.test.ts
  - src/components/plan/MealPickerSheet.tsx
  - src/components/plan/MealPickerSheet.test.tsx
  - src/components/plan/PlanBoard.tsx
  - src/db/seed.tsx
  - src/db/seed-data.ts
  - src/db/seed.test.ts
  - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VALIDATION.md
autonomous: true
requirements: [CAT-04, CAT-08]
must_haves:
  truths:
    - "Generator compatibility and explicit require-extra behavior resolve dynamic category IDs instead of literal category names"
    - "Meal picker filtering still respects extra/base compatibility with the new category-backed model"
    - "Fresh seeds boot with category rows, category-ID based defaults, and regression coverage aligned to the completed phase"
  artifacts:
    - path: "src/services/generator.ts"
      provides: "Runtime category-ID compatibility and require-extra resolution"
    - path: "src/components/plan/MealPickerSheet.tsx"
      provides: "Picker filtering that queries extras by base category ID"
    - path: "src/db/seed.tsx"
      provides: "Seed flow that creates category rows before seeded components and default rules"
    - path: "src/db/seed-data.ts"
      provides: "Seed fixtures expressed against category-backed setup helpers"
    - path: "src/db/seed.test.ts"
      provides: "Coverage for category-backed seed bootstrapping and default rules"
    - path: ".planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VALIDATION.md"
      provides: "Nyquist validation contract updated to the finished Phase 14 test surface"
  key_links:
    - from: "src/services/generator.ts"
      to: "src/types/plan.ts"
      via: "Require-extra runtime effects now carrying category IDs"
      pattern: "category_ids|require_extra"
    - from: "src/components/plan/MealPickerSheet.tsx"
      to: "src/services/food-db.ts"
      via: "Extra picker filtering by base category"
      pattern: "getExtrasByBaseCategoryId|getExtrasByBase"
    - from: "src/db/seed.tsx"
      to: "src/db/seed.test.ts"
      via: "Seeded category rows and ID-backed default rules"
      pattern: "categories|require_extra|base_category_id"
---

<objective>
Finish Phase 14 by pushing dynamic categories through runtime generation, picker filtering, and seed defaults, then lock the completed test surface into the phase validation contract.

Purpose: Per D-09 and CAT-04/CAT-08, the phase is not done until runtime behavior and fresh database bootstrap both work from category data instead of legacy literals.
Output: Dynamic-category generator and picker behavior, category-backed seeds/default rules, updated tests, and finalized Phase 14 validation metadata.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-CONTEXT.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-RESEARCH.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VALIDATION.md
@.planning/phases/13-only-include-extras-when-explicitly-required/13-01-SUMMARY.md
@src/services/generator.ts
@src/services/generator.test.ts
@src/components/plan/MealPickerSheet.tsx
@src/components/plan/MealPickerSheet.test.tsx
@src/db/seed.tsx
@src/db/seed-data.ts
@src/db/seed.test.ts

<interfaces>
From `src/services/generator.ts`:
```ts
export interface GenerateOptions {
  lockedSlots?: Partial<Record<`${DayOfWeek}-${MealSlot}`, {
    base_id?: number;
    curry_id?: number;
    subzi_id?: number;
    extra_ids?: number[];
  }>>;
}
```

From `src/components/plan/MealPickerSheet.tsx`:
```ts
interface MealPickerSheetProps {
  componentType: 'base' | 'curry' | 'subzi' | 'extras';
  currentBaseType?: BaseType;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate dynamic category IDs through generator and meal picker</name>
  <files>src/services/generator.ts, src/services/generator.test.ts, src/components/plan/MealPickerSheet.tsx, src/components/plan/MealPickerSheet.test.tsx, src/components/plan/PlanBoard.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
    - src/services/generator.ts
    - src/services/generator.test.ts
    - src/components/plan/MealPickerSheet.tsx
    - src/components/plan/MealPickerSheet.test.tsx
    - src/components/plan/PlanBoard.tsx
    - src/services/food-db.ts
    - src/types/plan.ts
    - src/types/component.ts
    - .planning/phases/13-only-include-extras-when-explicitly-required/13-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: per CAT-04, generator compatibility for extras and bases resolves category IDs, not legacy category-name literals.
    - Test 2: per CAT-04 and the Phase 13 contract, explicit `require_extra` remains the only runtime path that adds unlocked extras, but it now uses category IDs.
    - Test 3: per CAT-04, the meal picker still filters extras by the selected base category using the new service/query path.
  </behavior>
  <action>
    Update `src/services/generator.ts` so base selection, extra compatibility checks, and `require_extra` matching all work from category IDs produced by Plans 14-01 and 14-03. Preserve the Phase 13 rule that unlocked extras appear only from explicit `require_extra`, but convert the runtime effect payload and warning logic to category IDs. Handle deleted or uncategorized components defensively by skipping invalid matches rather than crashing.

    Update `src/components/plan/MealPickerSheet.tsx` so the extra picker receives a base category ID or equivalent category-aware context instead of the old `BaseType` literal. The extra query path must go through the updated `food-db` service and keep the current filter/search UI intact. Then update `src/components/plan/PlanBoard.tsx`, the active caller, so it derives the selected slot's base category ID from the chosen base component record and passes that category-backed context into the sheet.

    Rewrite `src/services/generator.test.ts` and `src/components/plan/MealPickerSheet.test.tsx` to prove category-ID compatibility, explicit-extra behavior continuity, deleted/uncategorized tolerance, and picker filtering against category-backed extras.
  </action>
  <acceptance_criteria>
    - `rg -n "category_ids|base_category_id|compatible_base_category_ids" src/services/generator.ts src/services/generator.test.ts` returns ID-based runtime coverage.
    - `rg -n "getExtrasByBaseCategoryId|currentBaseCategoryId|currentBaseType" src/components/plan/MealPickerSheet.tsx` returns a category-aware picker prop/query path and removes the old literal-only dependency.
    - `rg -n "currentBaseCategoryId|base_category_id" src/components/plan/PlanBoard.tsx` returns caller wiring that passes category-backed context into `MealPickerSheet`.
    - `rg -n "require_extra" src/services/generator.ts src/services/generator.test.ts` still returns the explicit-extra runtime path.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/generator.test.ts src/components/plan/MealPickerSheet.test.tsx</automated>
  </verify>
  <done>Runtime generation and picker filtering both honor the new category-backed model without regressing the explicit-extra behavior established in Phase 13.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed category-backed defaults and finalize the Phase 14 validation contract</name>
  <files>src/db/seed.tsx, src/db/seed-data.ts, src/db/seed.test.ts, .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VALIDATION.md</files>
  <read_first>
    - src/db/seed.tsx
    - src/db/seed-data.ts
    - src/db/seed.test.ts
    - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VALIDATION.md
    - src/services/category-db.ts
    - src/types/component.ts
    - src/types/plan.ts
    - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: per CAT-08, a fresh seed run inserts base and extra category rows before components/default rules that depend on them.
    - Test 2: per CAT-08, seeded components and seeded default rules persist category IDs instead of built-in literal category names.
    - Test 3: the finished validation contract names the real Phase 14 commands, sets `wave_0_complete: true`, sets `nyquist_compliant: true`, and records approval only after the listed checks pass.
  </behavior>
  <action>
    Update `src/db/seed-data.ts` and `src/db/seed.tsx` so the seed flow creates or looks up category rows first, then materializes seeded components/default rules using category IDs. Keep the current seeded library shape and existing Poori/default-rule behavior, but remove assumptions that categories are compile-time string unions.

    Rewrite `src/db/seed.test.ts` to assert seeded category rows exist, seeded bases/extras use category IDs, and seeded rules refer to category IDs rather than literal names.

    Update `.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VALIDATION.md` after the automated checks pass:
    - replace Wave 0 gaps with completed coverage references
    - update the per-task command map to match the implemented four-plan split
    - set `wave_0_complete: true`
    - set `nyquist_compliant: true`
    - set `status: approved`
  </action>
  <acceptance_criteria>
    - `rg -n "categories|base_category_id|extra_category_id|compatible_base_category_ids" src/db/seed.tsx src/db/seed-data.ts src/db/seed.test.ts` returns category-backed seed wiring.
    - `rg -n "status: approved|nyquist_compliant: true|wave_0_complete: true" .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VALIDATION.md` returns all 3 frontmatter states.
    - `rg -n "CAT-01|CAT-02|CAT-03|CAT-04|CAT-05|CAT-06|CAT-07|CAT-08" .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-VALIDATION.md` returns the full requirement map.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Fresh installs bootstrap category-backed data correctly, regression coverage matches the finished implementation, and Phase 14 validation metadata reflects a completed Nyquist-compliant phase.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/services/generator.test.ts src/components/plan/MealPickerSheet.test.tsx src/db/seed.test.ts`, then `npm test`. The phase only passes when runtime generation, picker filtering, and fresh seed boot all work from category data rather than hard-coded category literals.
</verification>

<success_criteria>
CAT-04 and CAT-08 are satisfied: generator/picker behavior is category-backed, explicit-extra semantics remain intact, and fresh databases seed categories plus default rules without any legacy literal-category persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-04-SUMMARY.md`
</output>
