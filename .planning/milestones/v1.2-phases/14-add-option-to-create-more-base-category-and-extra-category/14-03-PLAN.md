---
phase: 14-add-option-to-create-more-base-category-and-extra-category
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/types/plan.ts
  - src/services/rule-compiler.ts
  - src/components/rules/types.ts
  - src/components/rules/form-state.ts
  - src/components/rules/RuleFormFields/RuleFields.tsx
  - src/components/rules/ruleDescriptions.ts
  - src/components/rules/RuleRow.tsx
  - src/components/rules/form-state.test.ts
  - src/services/rule-compiler.test.ts
  - src/components/rules/ruleDescriptions.test.ts
autonomous: true
requirements: [CAT-03, CAT-06, CAT-07]
must_haves:
  truths:
    - "Rule targets and require-extra effects persist category IDs instead of category names"
    - "Rule forms render dynamic category-backed options for base-target and require-extra selections"
    - "Deleting a category leaves rules normalized and user-visible descriptions readable instead of exposing stale names or raw IDs"
  artifacts:
    - path: "src/types/plan.ts"
      provides: "Compiled rule schema updated for category-ID targets and require-extra effects"
    - path: "src/services/rule-compiler.ts"
      provides: "Compile/decompile logic that round-trips category-backed rule state"
    - path: "src/components/rules/types.ts"
      provides: "Rule form state with category-ID based target and require-extra fields"
    - path: "src/components/rules/RuleFormFields/RuleFields.tsx"
      provides: "Dynamic base-category and require-extra controls"
    - path: "src/components/rules/ruleDescriptions.ts"
      provides: "Label-resolved rule descriptions with deleted-category fallbacks"
    - path: "src/services/rule-compiler.test.ts"
      provides: "Round-trip coverage for category-ID based compile/decompile paths"
  key_links:
    - from: "src/components/rules/types.ts"
      to: "src/services/rule-compiler.ts"
      via: "Shared category-ID form contract"
      pattern: "base_category_id|require_extra_category_ids"
    - from: "src/services/rule-compiler.ts"
      to: "src/types/plan.ts"
      via: "Compiled rule schema for base targets and require-extra effects"
      pattern: "mode: 'base_category'|kind: 'require_extra'"
    - from: "src/components/rules/ruleDescriptions.ts"
      to: "src/lib/category-labels.ts"
      via: "Rename-safe and delete-safe label resolution"
      pattern: "getCategoryLabel|Deleted base category|Deleted extra category"
---

<objective>
Convert rules to the dynamic category model: schema, compiler/decompiler, form state, and descriptions must all move from literal category names to stable category IDs.

Purpose: Per D-09 and D-12, Phase 14 is incomplete if rules still encode base or extra categories as hard-coded strings after the component model becomes dynamic.
Output: Category-ID rule schema, rule form state and UI backed by live categories, description label resolution, direct `zod` dependency hygiene, and focused regression coverage.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-CONTEXT.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-RESEARCH.md
@.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-03-SUMMARY.md
@.planning/phases/13-only-include-extras-when-explicitly-required/13-01-SUMMARY.md
@src/types/plan.ts
@src/services/rule-compiler.ts
@src/components/rules/types.ts
@src/components/rules/form-state.ts
@src/components/rules/RuleFormFields/RuleFields.tsx
@src/components/rules/ruleDescriptions.ts
@src/lib/category-labels.ts
@src/services/category-db.ts

<interfaces>
From `src/types/plan.ts`:
```ts
export const TargetSchema = z.discriminatedUnion('mode', [
  z.object({ mode: z.literal('component_type'), component_type: z.enum(['base', 'curry', 'subzi']) }),
  z.object({ mode: z.literal('tag'), filter: TagFilterSchema }),
  z.object({ mode: z.literal('component'), component_id: z.number() }),
  z.object({ mode: z.literal('base_type'), base_type: z.enum(['rice-based', 'bread-based', 'other']) }),
]);

z.object({ kind: z.literal('require_extra'), categories: z.array(ExtraCategoryEnum) }),
```

From `src/components/rules/types.ts`:
```ts
export type TargetFormState =
  | { mode: 'component_type'; component_type: '' | 'base' | 'curry' | 'subzi' }
  | { mode: 'tag'; filter: TagFilter }
  | { mode: 'component'; component_id: number | null }
  | { mode: 'base_type'; base_type: '' | 'rice-based' | 'bread-based' | 'other' }
  | { mode: '' };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move rule persistence and form state to category IDs</name>
  <files>package.json, package-lock.json, src/types/plan.ts, src/services/rule-compiler.ts, src/components/rules/types.ts, src/components/rules/form-state.ts, src/services/rule-compiler.test.ts, src/components/rules/form-state.test.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
    - package.json
    - src/types/plan.ts
    - src/services/rule-compiler.ts
    - src/components/rules/types.ts
    - src/components/rules/form-state.ts
    - src/services/rule-compiler.test.ts
    - src/components/rules/form-state.test.ts
    - src/services/category-db.ts
    - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: per CAT-03, base-target rules compile to a category-ID target shape instead of `base_type` literals.
    - Test 2: per CAT-03, `require_extra` effects persist extra-category IDs rather than hard-coded names.
    - Test 3: per CAT-07, decompile/round-trip behavior drops deleted IDs from `require_extra` arrays and returns a valid normalized form state.
  </behavior>
  <action>
    If `zod` is still only transitively present, add it as a direct dependency in `package.json` and refresh `package-lock.json` before editing `src/types/plan.ts`. Then replace literal base and extra category enums in the rule schema with numeric ID-based shapes. Use explicit field names such as `base_category_id` on the target and `category_ids` on `require_extra` effects so the stored rule payload is unambiguous.

    Update `src/components/rules/types.ts`, `src/components/rules/form-state.ts`, and `src/services/rule-compiler.ts` so the editable rule contract uses category IDs all the way through compile/decompile. Preserve the existing explicit-extra semantics from Phases 12 and 13: `require_extra` stays the only extra-specific runtime effect, but its payload becomes ID-based. Normalize deleted IDs by stripping them from `require_extra` arrays during decompile and by returning an inert/disabled-ready state when a category-targeted rule no longer has a valid target.

    Rewrite `src/services/rule-compiler.test.ts` and `src/components/rules/form-state.test.ts` first so they fail on the old literal model and then prove category-ID round trips, normalization of deleted IDs, and direct-dependency hygiene for schema tooling.
  </action>
  <acceptance_criteria>
    - `rg -n "\"zod\"|zod" package.json src/types/plan.ts` returns a direct dependency entry in `package.json` and active imports in `src/types/plan.ts`.
    - `rg -n "base_category_id|category_ids" src/types/plan.ts src/services/rule-compiler.ts src/components/rules/types.ts` returns the ID-based rule fields.
    - `rg -n "base_type: 'rice-based'|categories: \\['condiment'" src/services/rule-compiler.test.ts src/components/rules/form-state.test.ts` returns no old literal category assertions.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/rule-compiler.test.ts src/components/rules/form-state.test.ts</automated>
  </verify>
  <done>Rule persistence, compile/decompile, and editable state all use category IDs, and the repo no longer relies on an undeclared transitive `zod` dependency for this schema layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render dynamic rule options and rename-safe descriptions</name>
  <files>src/components/rules/RuleFormFields/RuleFields.tsx, src/components/rules/ruleDescriptions.ts, src/components/rules/RuleRow.tsx, src/components/rules/ruleDescriptions.test.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
    - src/components/rules/RuleFormFields/RuleFields.tsx
    - src/components/rules/ruleDescriptions.ts
    - src/components/rules/RuleRow.tsx
    - src/components/rules/ruleDescriptions.test.ts
    - src/components/rules/types.ts
    - src/services/category-db.ts
    - src/lib/category-labels.ts
    - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: per CAT-03 and D-12, the rule form renders live base-category choices for the category-target mode and live extra-category choices for `require_extra`.
    - Test 2: per CAT-06, rule descriptions update automatically after rename because labels resolve from category records.
    - Test 3: per CAT-07, rule descriptions never show raw numeric IDs when a referenced category is deleted; they fall back to explicit deleted-category copy.
  </behavior>
  <action>
    Update `src/components/rules/RuleFormFields/RuleFields.tsx` to replace the hard-coded base-type radio group and extra-category toggle list with live category-backed options loaded from `src/services/category-db.ts`. Keep the existing target/effect UX shape, but the values moving through dispatch must be numeric IDs, not labels.

    Update `src/components/rules/ruleDescriptions.ts` to accept category context and resolve labels through `src/lib/category-labels.ts`. Use explicit fallbacks such as `Deleted base category` and `Deleted extra category`; do not display `component #id`-style category text for these surfaces. Then update `src/components/rules/RuleRow.tsx`, the active `describeRule()` caller, so it loads the needed category context and passes it into description rendering instead of continuing to call `describeRule(rule.compiled_filter)` with no label map.

    Rewrite `src/components/rules/ruleDescriptions.test.ts` to cover rename-safe label resolution and deleted-category fallbacks for both target and `require_extra` output.
  </action>
  <acceptance_criteria>
    - `rg -n "getCategoriesByKind|useLiveQuery" src/components/rules/RuleFormFields/RuleFields.tsx` returns live category-query wiring.
    - `rg -n "Deleted base category|Deleted extra category|getCategoryLabel" src/components/rules/ruleDescriptions.ts src/components/rules/ruleDescriptions.test.ts` returns all three strings.
    - `rg -n "describeRule\\(rule\\.compiled_filter,|useLiveQuery" src/components/rules/RuleRow.tsx` returns category-context caller wiring instead of a bare description call.
    - `rg -n "rice-based|bread-based|liquid|crunchy|condiment|dairy|sweet" src/components/rules/RuleFormFields/RuleFields.tsx` returns no hard-coded category option arrays.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/rule-compiler.test.ts src/components/rules/form-state.test.ts src/components/rules/ruleDescriptions.test.ts</automated>
  </verify>
  <done>Rules UI and descriptions are fully category-backed, rename-safe, and delete-safe without any remaining hard-coded category option lists.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/services/rule-compiler.test.ts src/components/rules/form-state.test.ts src/components/rules/ruleDescriptions.test.ts`. This plan only passes when rules store category IDs, the form renders live category options, and descriptions resolve rename/delete state without exposing stale names or raw IDs.
</verification>

<success_criteria>
CAT-03, CAT-06, and CAT-07 are satisfied for rules: category-backed rule persistence replaces hard-coded category names, rename cascades via live labels, and delete normalization leaves readable post-delete rule output.
</success_criteria>

<output>
After completion, create `.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-03-SUMMARY.md`
</output>
