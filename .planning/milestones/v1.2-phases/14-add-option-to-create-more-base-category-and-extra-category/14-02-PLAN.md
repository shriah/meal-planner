---
phase: 14-add-option-to-create-more-base-category-and-extra-category
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/library/CategoryManager.tsx
  - src/components/library/CategoryManager.test.tsx
  - src/components/library/ComponentLibrary.tsx
  - src/components/library/ComponentForm.tsx
  - src/components/library/ComponentForm.test.tsx
  - src/components/library/ComponentRow.tsx
autonomous: true
requirements: [CAT-02, CAT-05, CAT-06]
must_haves:
  truths:
    - "Users can open a separate category-management surface from the Library page and manage both base and extra categories there"
    - "Component create/edit uses live category-backed options instead of hard-coded base and extra lists"
    - "Renaming a category updates visible labels across library forms and rows without rewriting stored references"
  artifacts:
    - path: "src/components/library/CategoryManager.tsx"
      provides: "Separate sheet-based category management UI for both category kinds"
    - path: "src/components/library/ComponentLibrary.tsx"
      provides: "Library-page entry point for Manage Categories"
    - path: "src/components/library/ComponentForm.tsx"
      provides: "Dynamic category-backed selectors and compatibility checklist"
    - path: "src/components/library/ComponentRow.tsx"
      provides: "Resolved category labels for extras and compatibility display"
    - path: "src/components/library/CategoryManager.test.tsx"
      provides: "Coverage for add, rename, delete, and empty/loading states"
    - path: "src/components/library/ComponentForm.test.tsx"
      provides: "Coverage for dynamic category options in base and extra forms"
  key_links:
    - from: "src/components/library/ComponentLibrary.tsx"
      to: "src/components/library/CategoryManager.tsx"
      via: "Manage Categories header action"
      pattern: "Manage Categories|CategoryManager"
    - from: "src/components/library/CategoryManager.tsx"
      to: "src/services/category-db.ts"
      via: "Live category CRUD flows"
      pattern: "addCategory|renameCategory|deleteCategory|getCategoriesByKind"
---

<objective>
Deliver the Library-side Phase 14 UX: a dedicated category manager and dynamic category-backed component authoring that keeps the existing compatibility checklist interaction.

Purpose: Per D-01, D-02, D-03, and D-11, category creation/edit/delete must be clearly separate from the component form while still flowing into the existing library authoring surfaces immediately.
Output: Category manager sheet, library entry point, dynamic component form options, and UI tests for add/rename/delete plus category-backed form behavior.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-CONTEXT.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-RESEARCH.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-UI-SPEC.md
@.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-01-PLAN.md
@src/components/library/ComponentLibrary.tsx
@src/components/library/ComponentForm.tsx
@src/components/library/ComponentRow.tsx
@src/components/library/DeleteConfirmStrip.tsx
@src/services/category-db.ts
@src/lib/category-labels.ts

<interfaces>
From `src/services/category-db.ts`:
```ts
export async function getCategoriesByKind(kind: CategoryKind): Promise<CategoryRecord[]>;
export async function addCategory(input: { kind: CategoryKind; name: string }): Promise<number>;
export async function renameCategory(id: number, name: string): Promise<void>;
export async function deleteCategory(id: number): Promise<void>;
```

From `src/types/component.ts`:
```ts
export type ComponentType = 'base' | 'curry' | 'subzi' | 'extra';
export type ComponentRecord = BaseComponentFields & {
  base_category_id?: number | null;
  extra_category_id?: number | null;
  compatible_base_category_ids?: number[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the separate category-management sheet for both category kinds</name>
  <files>src/components/library/CategoryManager.tsx, src/components/library/CategoryManager.test.tsx, src/components/library/ComponentLibrary.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
    - src/components/library/ComponentLibrary.tsx
    - src/components/library/DeleteConfirmStrip.tsx
    - src/components/rules/RuleRow.tsx
    - src/components/ui/sheet.tsx
    - src/components/ui/tabs.tsx
    - src/services/category-db.ts
    - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-UI-SPEC.md
    - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: per CAT-05 and D-01/D-02, the Library header exposes `Manage Categories` and the opened sheet shows both `Base Categories` and `Extra Categories`.
    - Test 2: per D-03, the active section supports inline add and rename flows with `Add Category`, `Save Name`, and `Stop Renaming`.
    - Test 3: per D-05 and D-14, deleting an in-use category is allowed and routes through a destructive confirmation that says cleanup is automatic.
  </behavior>
  <action>
    Add `src/components/library/CategoryManager.tsx` as a client component using the existing `Sheet`, `Tabs`, `Button`, `Input`, and `Separator` primitives. Follow `14-UI-SPEC.md` exactly: one sheet, two tabs/sections, one `Add Category` control at the top of the active kind list, inline draft row insertion, single-row rename mode, inline destructive confirmation under the row, and no inline creation inside `ComponentForm`.

    Update `src/components/library/ComponentLibrary.tsx` to add a secondary `Manage Categories` action beside the `Component Library` title. The page stays visually aligned with the current library page; do not create a new route or settings page in this plan.

    Create `src/components/library/CategoryManager.test.tsx` with happy-dom coverage for opening the sheet, switching between base/extra sections, add/rename/delete interactions, duplicate-name validation copy (`Enter a unique category name.`), and the empty/loading placeholders required by the UI spec.
  </action>
  <acceptance_criteria>
    - `rg -n "Manage Categories|Base Categories|Extra Categories" src/components/library/ComponentLibrary.tsx src/components/library/CategoryManager.tsx` returns all three labels.
    - `rg -n "Add Category|Save Name|Stop Renaming|Delete .*cleanup is automatic" src/components/library/CategoryManager.tsx src/components/library/CategoryManager.test.tsx` returns the UI-spec copy.
    - `test -f src/components/library/CategoryManager.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/library/CategoryManager.test.tsx</automated>
  </verify>
  <done>The Library page exposes a separate category manager and both category kinds can be added, renamed, and deleted there without mixing category creation into the component form.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire component authoring and row display to dynamic category data</name>
  <files>src/components/library/ComponentForm.tsx, src/components/library/ComponentForm.test.tsx, src/components/library/ComponentRow.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md
    - src/components/library/ComponentForm.tsx
    - src/components/library/ComponentRow.tsx
    - src/services/food-db.ts
    - src/services/category-db.ts
    - src/lib/category-labels.ts
    - src/types/component.ts
    - .planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: per CAT-02, base create/edit uses live base-category records instead of the literal `rice-based` / `bread-based` / `other` radio set.
    - Test 2: per CAT-02 and D-11, extra create/edit uses live extra-category options plus a live multi-select compatibility checklist for base categories.
    - Test 3: per CAT-06, renamed categories immediately change visible labels in the form and row display because labels are resolved from category rows, not stored strings.
  </behavior>
  <action>
    Update `src/components/library/ComponentForm.tsx` to load categories by kind with live queries and save `base_category_id`, `extra_category_id`, and `compatible_base_category_ids`. Preserve the current multi-select checklist feel for compatibility per D-11. When category data is still loading, keep the form shell visible and avoid blocking the whole page with a spinner.

    Update `src/components/library/ComponentRow.tsx` so extra rows display resolved extra-category and compatible-base labels through `src/lib/category-labels.ts` instead of joining stored literal strings. Keep the existing delete affordance and inline editor behavior unchanged.

    Add `src/components/library/ComponentForm.test.tsx` to prove that the form renders dynamic options from category records, persists numeric IDs on save, and refreshes labels after a mocked rename without form code translating IDs back into stored names.
  </action>
  <acceptance_criteria>
    - `rg -n "getCategoriesByKind|useLiveQuery" src/components/library/ComponentForm.tsx` returns a live category query path.
    - `rg -n "base_category_id|extra_category_id|compatible_base_category_ids" src/components/library/ComponentForm.tsx src/components/library/ComponentForm.test.tsx` returns all 3 persisted fields.
    - `rg -n "getCategoryLabel|buildCategoriesById" src/components/library/ComponentRow.tsx` returns label-resolution helpers.
    - `test -f src/components/library/ComponentForm.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/library/CategoryManager.test.tsx src/components/library/ComponentForm.test.tsx</automated>
  </verify>
  <done>Library component create/edit and row rendering are driven by live category records, while category add/rename/delete remains isolated to the separate manager surface.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/components/library/CategoryManager.test.tsx src/components/library/ComponentForm.test.tsx`. The plan only passes when the Library header opens a separate category manager and all base/extra category controls read from live category records instead of hard-coded arrays.
</verification>

<success_criteria>
CAT-02, CAT-05, and CAT-06 are satisfied on the Library side: category management is separate, component authoring uses live category records, and rename cascades by label resolution rather than data rewrites.
</success_criteria>

<output>
After completion, create `.planning/phases/14-add-option-to-create-more-base-category-and-extra-category/14-02-SUMMARY.md`
</output>
