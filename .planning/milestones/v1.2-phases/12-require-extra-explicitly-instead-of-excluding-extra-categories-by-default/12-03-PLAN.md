---
phase: 12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - src/types/plan.ts
  - src/services/generator.ts
  - src/services/generator.test.ts
  - .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md
autonomous: true
requirements: [PH12-04]
must_haves:
  truths:
    - "Meal-template rules with no required extras leave extras otherwise unconstrained"
    - "The generator emits extra-related warnings only when an explicit require-extra category has no eligible extras"
    - "Nyquist validation reflects the post-phase require-or-none test surface and sign-off state"
  artifacts:
    - path: "src/types/plan.ts"
      provides: "Compiled rule effect schema without active exclude-extra support"
    - path: "src/services/generator.ts"
      provides: "Require-extra-or-none generator logic"
    - path: "src/services/generator.test.ts"
      provides: "Regression coverage for unconstrained extras and explicit require-extra warnings"
    - path: ".planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md"
      provides: "Updated Nyquist contract for the post-phase test surface"
  key_links:
    - from: "src/types/plan.ts"
      to: "src/services/generator.ts"
      via: "RequireExtraEffect-only runtime effect handling"
      pattern: "RequireExtraEffect|EffectSchema"
    - from: "src/services/generator.ts"
      to: "src/services/generator.test.ts"
      via: "Require-or-none runtime semantics"
      pattern: "require_extra|warnings|eligibleExtras"
---

<objective>
Finish Phase 12 by removing remaining runtime exclude-extra support from the effect schema and generator, then update generator tests and the validation contract so execution and verification reflect the final require-or-none behavior.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-CONTEXT.md
@.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-RESEARCH.md
@.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md
@.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove runtime exclude-extra support and implement require-extra-or-none generator semantics</name>
  <files>src/types/plan.ts, src/services/generator.ts</files>
  <read_first>
    - src/types/plan.ts
    - src/services/generator.ts
    - src/db/client.ts
    - src/services/rule-compiler.ts
    - .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-CONTEXT.md
    - .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-RESEARCH.md
    - .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `EffectSchema` and exported effect types no longer expose `exclude_extra`.
    - Test 2: `generate()` never filters extras by excluded categories when no required extra is configured.
    - Test 3: Explicit `require_extra` warning text remains the only extra-related warning path.
  </behavior>
  <action>
    In `src/types/plan.ts`, remove the `exclude_extra` variant from `EffectSchema`, delete `ExcludeExtraEffect`, and update the exported composition-effect unions so `require_extra` is the only extra-specific effect.

    In `src/services/generator.ts`, remove `ExcludeExtraEffect` imports and delete the runtime branch that computes excluded extra categories or emits exclude-extra relaxation warnings. Keep the existing compatible-base-type and occasion filters, the required-extra de-duplication logic, and the existing warning text for unsatisfied explicit `require_extra`.
  </action>
  <acceptance_criteria>
    - `rg -n "exclude_extra|ExcludeExtraEffect" src/types/plan.ts src/services/generator.ts` returns no matches.
    - `rg -n "kind === 'require_extra'|require_extra category '\\$\\{category\\}' has no eligible extras" src/services/generator.ts` returns both patterns.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/generator.test.ts</automated>
  </verify>
  <done>Runtime effect handling is require-extra-only and no exclude-extra generator path remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite generator coverage and finalize Nyquist validation state</name>
  <files>src/services/generator.test.ts, .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md</files>
  <read_first>
    - src/services/generator.test.ts
    - src/services/generator.ts
    - src/types/plan.ts
    - .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: Generator tests no longer reference `TMPL-04`, `exclude_extra`, or `constraint relaxed` behavior.
    - Test 2: A rule with no `require_extra` leaves extras unconstrained and produces no extra-related warning.
    - Test 3: The validation contract reflects the post-phase generator command, sets `wave_0_complete: true`, sets `nyquist_compliant: true`, and records approval once the listed checks pass.
  </behavior>
  <action>
    Rewrite `src/services/generator.test.ts` to remove obsolete extra-exclusion cases and replace them with require-or-none coverage. Keep the explicit require-extra success and unsatisfied-warning cases.

    Update `.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md` after the tests are green:
    - remove all `TMPL-04` / `exclude_extra` wording from the verification map and wave-0 bullets
    - update the generator verification command so it targets require-or-none behavior only
    - set frontmatter `wave_0_complete: true` and `nyquist_compliant: true`
    - set `Approval: approved 2026-03-28`

    Run the listed validation commands and `npm test` as the phase gate.
  </action>
  <acceptance_criteria>
    - `rg -n "TMPL-04|exclude_extra|constraint relaxed" src/services/generator.test.ts .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md` returns no matches.
    - `rg -n "nyquist_compliant: true|wave_0_complete: true|Approval: approved 2026-03-28" .planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-VALIDATION.md` returns all 3 matches.
    - `npx vitest run src/services/rule-compiler.test.ts src/db/migrations.test.ts src/components/rules/form-state.test.ts src/components/rules/ruleDescriptions.test.ts` exits 0.
    - `npx vitest run src/services/generator.test.ts src/components/rules/RuleForm.test.tsx src/components/rules/RuleRow.test.tsx` exits 0.
    - `npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Generator regression coverage and Nyquist validation both reflect the final require-or-none semantics.</done>
</task>

</tasks>

<verification>
Run the two validation waves listed in `12-VALIDATION.md`, then `npm test`. The phase is only complete when no runtime code, tests, or Nyquist metadata still reference exclude-extra behavior.
</verification>

<success_criteria>
PH12-04 is satisfied: extras are unconstrained by default, explicit require-extra still works, extra warnings only occur for unsatisfied explicit requirements, and the validation contract reflects the completed phase.
</success_criteria>

<output>
After completion, create `.planning/phases/12-require-extra-explicitly-instead-of-excluding-extra-categories-by-default/12-03-SUMMARY.md`
</output>
