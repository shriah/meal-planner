---
phase: 11-edit-rule
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/app/layout.tsx
  - src/components/rules/EditRuleSheet.tsx
  - src/components/rules/RuleRow.tsx
  - src/components/rules/RuleRow.test.tsx
  - src/services/food-db.test.ts
autonomous: true
requirements: [EDIT-01, EDIT-03, EDIT-04]
must_haves:
  truths:
    - "Each existing rule row exposes an Edit button that opens a right-side Sheet without leaving /rules"
    - "The sheet opens with the selected rule’s name, target, scope, and effects already populated from Dexie data"
    - "Saving edits updates the original Dexie record in place and does not increase the total rule count"
    - "Discard Changes, the built-in close button, and Escape all close the sheet without persisting draft edits"
    - "If updateRule fails, the sheet stays open and the user sees the exact failure copy from the UI spec"
  artifacts:
    - path: "src/components/rules/EditRuleSheet.tsx"
      provides: "Controlled edit sheet using shared form state, compile/decompile, and updateRule"
      exports: ["EditRuleSheet"]
    - path: "src/components/rules/RuleRow.tsx"
      provides: "Edit button placement, edit-sheet open state, save/discard wiring"
      contains: "aria-label=\"Edit rule\""
    - path: "src/components/rules/RuleRow.test.tsx"
      provides: "Interaction coverage for open, prepopulate, save overwrite, and discard/reset"
    - path: "src/services/food-db.test.ts"
      provides: "Row-count-preserving updateRule coverage"
      contains: "updateRule"
    - path: "src/app/layout.tsx"
      provides: "Global toaster mount for save-failure feedback"
      contains: "Toaster"
  key_links:
    - from: "src/components/rules/RuleRow.tsx"
      to: "src/components/rules/EditRuleSheet.tsx"
      via: "local open state and selected rule prop"
      pattern: "EditRuleSheet"
    - from: "src/components/rules/EditRuleSheet.tsx"
      to: "src/services/rule-compiler.ts"
      via: "decompileRule on open and compileRule on save"
      pattern: "decompileRule|compileRule"
    - from: "src/components/rules/EditRuleSheet.tsx"
      to: "src/services/food-db.ts"
      via: "updateRule(rule.id!, changes)"
      pattern: "updateRule\\(rule\\.id!"
    - from: "src/app/layout.tsx"
      to: "sonner"
      via: "global Toaster provider"
      pattern: "Toaster"
---

<objective>
Wire the actual edit experience into the rules list by adding an edit button, a controlled sheet, overwrite-in-place save logic, discard-on-close semantics, and a visible failure surface for save errors.

Purpose: This plan fulfills the user-facing requirements after the reversible state contract from Plan 11-01 exists. It keeps the risky persistence and close/reset behavior together, separate from the compiler/reducer groundwork.
Output: Edit button on each rule row, `EditRuleSheet` reuse of shared form internals, save via `updateRule`, discard behavior across all close paths, and automated tests for open/save/discard flows.
</objective>

<execution_context>
@/Users/harish/.codex/get-shit-done/workflows/execute-plan.md
@/Users/harish/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-edit-rule/11-RESEARCH.md
@.planning/phases/11-edit-rule/11-VALIDATION.md
@.planning/phases/11-edit-rule/11-UI-SPEC.md
@.planning/phases/05-rules-manager-ui/05-01-SUMMARY.md
@.planning/phases/11-edit-rule/11-01-PLAN.md
@src/components/rules/RuleRow.tsx
@src/components/ui/sheet.tsx
@src/app/layout.tsx
@src/services/food-db.ts

<interfaces>
From src/db/client.ts:
```typescript
export interface RuleRecord {
  id?: number;
  name: string;
  enabled: boolean;
  compiled_filter: CompiledRule;
  created_at: string;
}
```

From src/services/food-db.ts:
```typescript
export async function updateRule(id: number, changes: Partial<RuleRecord>): Promise<void>;
export async function getRules(): Promise<RuleRecord[]>;
```

From src/services/rule-compiler.ts after Plan 11-01:
```typescript
export function compileRule(state: RuleFormState): CompiledRule;
export function decompileRule(compiled: CompiledRule, name: string): RuleFormState;
```

From src/components/rules/form-state.ts after Plan 11-01:
```typescript
export const EMPTY_RULE_FORM_STATE: RuleFormState;
export function formReducer(state: RuleFormState, action: FormAction): RuleFormState;
export function isFormValid(state: RuleFormState): boolean;
```

From src/components/ui/sheet.tsx:
```typescript
function Sheet(props: React.ComponentProps<typeof SheetPrimitive.Root>)
function SheetContent(props: React.ComponentProps<typeof SheetPrimitive.Content> & {
  side?: "top" | "right" | "bottom" | "left"
  showCloseButton?: boolean
})
function SheetHeader(props: React.ComponentProps<"div">)
function SheetFooter(props: React.ComponentProps<"div">)
function SheetTitle(props: React.ComponentProps<typeof SheetPrimitive.Title>)
function SheetDescription(props: React.ComponentProps<typeof SheetPrimitive.Description>)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UI and persistence tests for edit open, overwrite, discard, and reset behavior</name>
  <files>src/components/rules/RuleRow.test.tsx, src/services/food-db.test.ts</files>
  <read_first>
    - src/components/rules/RuleRow.tsx
    - src/services/food-db.test.ts
    - src/services/food-db.ts
    - src/services/rule-compiler.ts
    - src/components/ui/sheet.tsx
    - src/test/setup.ts
    - vitest.config.ts
    - node_modules/next/dist/docs/index.md
    - node_modules/next/dist/docs/01-app/index.md
    - .planning/phases/11-edit-rule/11-VALIDATION.md
    - .planning/phases/11-edit-rule/11-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: Clicking the new edit icon button opens a right-side sheet and shows the selected rule’s current name and effect controls.
    - Test 2: Saving from the sheet calls the overwrite path and leaves the number of persisted rules unchanged.
    - Test 3: Clicking `Discard Changes`, pressing Escape, or using the close button closes the sheet without calling `updateRule`, and reopening rehydrates from the original rule instead of the abandoned draft.
    - Test 4: If `updateRule` rejects, the sheet remains open and the error copy `Failed to save rule. Please try again.` is emitted.
  </behavior>
  <action>
    Create `src/components/rules/RuleRow.test.tsx` with a `/** @vitest-environment happy-dom */` docblock because `vitest.config.ts` defaults to `environment: 'node'`. Use React Testing Library and `user-event`.

    Seed the minimum IndexedDB data needed for live queries inside the test file, or mock the service functions that `RuleRow` / `EditRuleSheet` depend on. Cover these exact cases:
    - open the sheet from an edit button with tooltip/aria label `Edit rule`
    - verify the rule name input is prefilled and at least one pre-existing effect control is active
    - change the name, save, then assert the row text updates without creating a second record
    - change the name, discard, reopen, and assert the original name is restored
    - trigger close via Escape and via the built-in close button
    - mock an `updateRule` rejection and assert the exact toast/error text appears while the sheet stays open

    Extend `src/services/food-db.test.ts` with an `updateRule` coverage block for EDIT-03:
    - add one rule with `addRule`
    - read `getRules()` before update and after update
    - assert `before.length === after.length === 1`
    - assert `after[0].id === before[0].id`
    - assert the updated `name` and `compiled_filter` changed as expected while `created_at` stays intact

    Keep these tests failing until Task 2 implements the edit UI and save path.
  </action>
  <acceptance_criteria>
    - `rg -n "@vitest-environment happy-dom|Edit rule|Discard Changes|Failed to save rule\\. Please try again\\." src/components/rules/RuleRow.test.tsx` returns all 4 required strings.
    - `rg -n "before\\.length|after\\.length|after\\[0\\]\\.id === before\\[0\\]\\.id|created_at" src/services/food-db.test.ts` returns matches for overwrite semantics.
    - `npm test -- src/services/food-db.test.ts src/components/rules/RuleRow.test.tsx` exits non-zero before Task 2 implementation and exits 0 after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/services/food-db.test.ts src/components/rules/RuleRow.test.tsx</automated>
  </verify>
  <done>Regression tests exist for edit open/save/discard/failure flows and for in-place Dexie overwrite semantics, giving Task 2 a concrete pass/fail target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement EditRuleSheet, row-level edit action, overwrite save, discard reset, and failure toast</name>
  <files>package.json, package-lock.json, src/app/layout.tsx, src/components/rules/EditRuleSheet.tsx, src/components/rules/RuleRow.tsx</files>
  <read_first>
    - package.json
    - src/app/layout.tsx
    - src/components/rules/RuleRow.tsx
    - src/components/rules/form-state.ts
    - src/components/rules/RuleImpactPreview.tsx
    - src/components/rules/RuleFormFields/RuleFields.tsx
    - src/components/ui/button.tsx
    - src/components/ui/input.tsx
    - src/components/ui/label.tsx
    - src/components/ui/sheet.tsx
    - src/components/ui/tooltip.tsx
    - node_modules/next/dist/docs/index.md
    - node_modules/next/dist/docs/01-app/index.md
    - src/services/rule-compiler.ts
    - src/services/food-db.ts
    - .planning/phases/11-edit-rule/11-RESEARCH.md
    - .planning/phases/11-edit-rule/11-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `RuleRow` renders a ghost icon button with `Pencil`, `aria-label="Edit rule"`, and tooltip copy `Edit rule`, placed between the enabled toggle and delete button.
    - Test 2: Opening the sheet dispatches `LOAD_PRESET` with `decompileRule(rule.compiled_filter, rule.name)` and shows `Edit Rule`, `Changes are saved when you click Save Rule.`, and a `Save Rule` primary button.
    - Test 3: Saving calls `updateRule(rule.id!, { name: state.name.trim(), compiled_filter: compileRule(state), enabled: rule.enabled })`, closes the sheet on success, and leaves `created_at` untouched because it is not overwritten.
    - Test 4: Closing paths only toggle `open` false; reopening always reseeds from the original rule prop so abandoned drafts never persist in local component state.
  </behavior>
  <action>
    Install `sonner` and commit the package manifest updates in this task. Mount `<Toaster />` once in `src/app/layout.tsx` inside `<body>` after `<SeedBootstrap>` content so the edit-sheet save failure can render a global toast.

    Create `src/components/rules/EditRuleSheet.tsx` as a controlled client component:
    - props: `{ rule: RuleRecord; open: boolean; onOpenChange: (open: boolean) => void }`
    - imports: `useEffect`, `useReducer`, `useState`, `toast` from `sonner`, shared state exports from `./form-state`, `compileRule` and `decompileRule`, `updateRule`, `RuleFields`, `RuleImpactPreview`, and shadcn `Sheet` primitives
    - initialize reducer with `EMPTY_RULE_FORM_STATE`
    - when `open` becomes `true`, dispatch `LOAD_PRESET` with `decompileRule(rule.compiled_filter, rule.name)`; include `rule` in dependencies so the seed changes if a different row opens the same shared component instance
    - local `saving` boolean gates duplicate submits
    - `handleSave` must no-op when `!isFormValid(state) || saving || rule.id == null`
    - success path: `await updateRule(rule.id, { name: state.name.trim(), compiled_filter: compileRule(state), enabled: rule.enabled })` then `onOpenChange(false)`
    - failure path: `toast.error('Failed to save rule. Please try again.')`, keep the sheet open, and re-enable save

    Match the UI contract exactly in `EditRuleSheet.tsx`:
    - `Sheet open={open} onOpenChange={onOpenChange}`
    - `SheetContent side="right"`
    - `SheetHeader` with title `Edit Rule` and description `Changes are saved when you click Save Rule.`
    - scrollable body with rule-name `Input`, `RuleFields`, and `RuleImpactPreview`
    - `SheetFooter` with primary button copy `Save Rule` / `Saving...` and ghost secondary button `Discard Changes`
    - discard button uses `type="button"` and only calls `onOpenChange(false)`

    Update `src/components/rules/RuleRow.tsx`:
    - add local `editing` state
    - render the new edit button between the switch and delete button per EDIT-01 / UI spec
    - use `Pencil` from `lucide-react`
    - keep tooltip pattern consistent with delete
    - render `<EditRuleSheet rule={rule} open={editing} onOpenChange={setEditing} />`
    - keep existing toggle and delete behavior unchanged

    Do not navigate away from `/rules` in this task. Do not create a separate route. Do not use `addRule` anywhere in the edit flow.
  </action>
  <acceptance_criteria>
    - `rg -n "\"sonner\"|Toaster" package.json src/app/layout.tsx` returns matches proving the dependency and global toaster mount exist.
    - `rg -n "toast\\.error\\('Failed to save rule\\. Please try again\\.'\\)|decompileRule\\(rule\\.compiled_filter, rule\\.name\\)|updateRule\\(rule\\.id" src/components/rules/EditRuleSheet.tsx` returns all 3 key patterns.
    - `rg -n "aria-label=\"Edit rule\"|<Pencil|<EditRuleSheet|setEditing\\(true\\)" src/components/rules/RuleRow.tsx` returns matches for the row wiring.
    - `npm test -- src/services/food-db.test.ts src/components/rules/RuleRow.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/services/food-db.test.ts src/components/rules/RuleRow.test.tsx</automated>
  </verify>
  <done>The rules list exposes an edit sheet, edits overwrite the existing Dexie record in place, all close paths discard drafts, and save failures surface the exact UI-spec copy without leaving the page.</done>
</task>

</tasks>

<verification>
Run `npm test -- src/services/rule-compiler.test.ts src/components/rules/form-state.test.ts src/services/food-db.test.ts src/components/rules/RuleRow.test.tsx` for focused coverage, then `npm test` for the phase wave gate. Manually inspect that `/rules` still loads and that edit remains inline with no route transition.
</verification>

<success_criteria>
Users can open an edit sheet from any rule row, see the current rule state without blank fields, save in place without creating duplicates, and close or cancel without leaking unsaved edits across reopen cycles.
</success_criteria>

<output>
After completion, create `.planning/phases/11-edit-rule/11-02-SUMMARY.md`.
</output>
